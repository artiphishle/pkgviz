import type { StylesheetJson } from 'cytoscape';

// No specific 'breadthfirst' styles for now
/*** Returns Cytoscape style overrides for this layout. */
export function getStyle() {
  const style: StylesheetJson = [];

  return style;
}
