import { findNestedKey } from './findNestedKey.js';
/*** Throw when an unknown nested value contains any forbidden record key. */
export function assertNoNestedKeys(value, forbiddenKeys, createMessage) {
    const match = findNestedKey(value, forbiddenKeys);
    if (match !== null)
        throw new Error(createMessage(match));
}
//# sourceMappingURL=assertNoNestedKeys.js.map