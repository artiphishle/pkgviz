/*** Read an own property without traversing the prototype chain. */
export function readOwnProperty(target, key) {
    if (!Object.hasOwn(target, key))
        return undefined;
    return Reflect.get(target, key);
}
//# sourceMappingURL=readOwnProperty.js.map