/*** Find the first item whose derived key equals a requested key. */
export function findByKey(values, key, keyOf, equals = Object.is) {
    return values.find((value) => equals(keyOf(value), key));
}
//# sourceMappingURL=findByKey.js.map