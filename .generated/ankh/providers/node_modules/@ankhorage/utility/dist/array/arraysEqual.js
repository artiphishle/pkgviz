/*** Return whether two arrays contain equal values in the same order according to a comparator. */
export function arraysEqual(left, right, equals = Object.is) {
    return (left.length === right.length &&
        left.every((value, index) => equals(value, right.at(index), index)));
}
//# sourceMappingURL=arraysEqual.js.map