/*** Filter values through a type-guard predicate while preserving the narrowed result type. */
export function filterBy(values, predicate) {
    return values.filter(predicate);
}
//# sourceMappingURL=filterBy.js.map