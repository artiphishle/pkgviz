/*** Join a base URL and relative request path without duplicate boundary slashes. */
export function joinBaseUrl(baseUrl, path) {
    return `${baseUrl.replace(/\/+$/u, '')}/${path.replace(/^\/+/, '')}`;
}
//# sourceMappingURL=joinBaseUrl.js.map