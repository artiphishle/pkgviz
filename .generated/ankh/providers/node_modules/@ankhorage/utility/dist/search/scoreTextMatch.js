/*** Score an already-normalized target by exact, prefix, or substring match quality. */
export function scoreTextMatch(target, query) {
    if (query.length === 0)
        return 0;
    if (target === query)
        return 100;
    if (target.startsWith(query))
        return 50;
    return target.includes(query) ? 25 : 0;
}
//# sourceMappingURL=scoreTextMatch.js.map