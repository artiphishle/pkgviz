const DYNAMIC_ROUTE_SEGMENT_PATTERN = /^\[[^.[\]]+\]$|^:[^/]+$/u;
const CATCH_ALL_ROUTE_SEGMENT_PATTERN = /^\[\.\.\.[^\]]+\]$/u;
const OPTIONAL_CATCH_ALL_ROUTE_SEGMENT_PATTERN = /^\[\[\.\.\.[^\]]+\]\]$/u;
/*** Score an exact, dynamic, catch-all, or optional-catch-all route match against pathname segments. */
export function scoreRoutePatternMatch(pattern, pathname) {
    let patternIndex = 0;
    let pathnameIndex = 0;
    let score = 0;
    let exactMatch = true;
    while (patternIndex < pattern.length) {
        const routeSegment = pattern.at(patternIndex);
        if (routeSegment === undefined)
            return null;
        if (OPTIONAL_CATCH_ALL_ROUTE_SEGMENT_PATTERN.test(routeSegment)) {
            exactMatch = false;
            if (pathnameIndex < pathname.length)
                score += 1;
            pathnameIndex = pathname.length;
            patternIndex += 1;
            continue;
        }
        if (CATCH_ALL_ROUTE_SEGMENT_PATTERN.test(routeSegment)) {
            if (pathnameIndex >= pathname.length)
                return null;
            exactMatch = false;
            pathnameIndex = pathname.length;
            patternIndex += 1;
            score += 1;
            continue;
        }
        const pathnameSegment = pathname.at(pathnameIndex);
        if (pathnameSegment === undefined)
            return null;
        if (DYNAMIC_ROUTE_SEGMENT_PATTERN.test(routeSegment))
            score += 10;
        else if (routeSegment === pathnameSegment)
            score += 100;
        else
            return null;
        patternIndex += 1;
        pathnameIndex += 1;
    }
    if (pathnameIndex !== pathname.length)
        return null;
    return score + (exactMatch ? 5 : 0);
}
//# sourceMappingURL=scoreRoutePatternMatch.js.map