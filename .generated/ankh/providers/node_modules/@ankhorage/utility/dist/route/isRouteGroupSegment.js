/*** Return whether a route segment is an Expo-style grouping segment. */
export function isRouteGroupSegment(segment) {
    return segment.startsWith('(') && segment.endsWith(')');
}
//# sourceMappingURL=isRouteGroupSegment.js.map