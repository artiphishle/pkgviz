/*** Read a non-empty own string environment variable from a Node-style environment record. */
export function readEnvString(name, environment = process.env) {
    if (!Object.hasOwn(environment, name))
        return undefined;
    const value = Reflect.get(environment, name);
    return typeof value === 'string' && value.length > 0 ? value : undefined;
}
//# sourceMappingURL=readEnvString.js.map