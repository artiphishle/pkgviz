import { promises as fs } from 'node:fs';
import { isMissingPathError } from './isMissingPathError.js';
/*** Assert that readable text files do not match a forbidden pattern, optionally ignoring missing files. */
export async function assertTextFilesDoNotMatch(filePaths, pattern, options = {}) {
    for (const filePath of filePaths) {
        let source;
        try {
            source = await fs.readFile(filePath, 'utf8');
        }
        catch (error) {
            if (options.ignoreMissing !== false && isMissingPathError(error))
                continue;
            throw error;
        }
        pattern.lastIndex = 0;
        if (pattern.test(source)) {
            throw new Error(`File '${filePath}' matches a forbidden pattern.`);
        }
    }
}
//# sourceMappingURL=assertTextFilesDoNotMatch.js.map