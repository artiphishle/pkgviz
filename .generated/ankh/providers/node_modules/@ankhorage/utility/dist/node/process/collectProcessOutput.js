/*** Collect stdout and stderr chunks from a child process into one ordered string buffer. */
export function collectProcessOutput(processToCollect, output) {
    processToCollect.stdout?.on('data', (chunk) => output.push(String(chunk)));
    processToCollect.stderr?.on('data', (chunk) => output.push(String(chunk)));
}
//# sourceMappingURL=collectProcessOutput.js.map