import { type Dirent } from 'node:fs';
/*** Reads directory entries only when the directory stays inside the allowed filesystem root. */
export declare function readDirectoryWithinRoot(args: {
    readonly rootPath: string;
    readonly directoryPath: string;
}): {
    readonly entries: Dirent[];
    readonly path: string;
};
//# sourceMappingURL=readDirectoryWithinRoot.d.ts.map