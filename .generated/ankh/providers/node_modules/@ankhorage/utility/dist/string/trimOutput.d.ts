import type { TrimmedText } from './types.js';
/*** Limit text to a maximum character count while preserving original length and reporting truncation. */
export declare function trimOutput(text: string, maxChars: number, createMarker?: (omittedCharacters: number) => string): TrimmedText;
//# sourceMappingURL=trimOutput.d.ts.map