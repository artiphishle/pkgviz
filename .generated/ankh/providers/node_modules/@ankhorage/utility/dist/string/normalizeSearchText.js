/*** Normalize text for case-insensitive searching and comparison. */
export function normalizeSearchText(value) {
    return value.trim().toLocaleLowerCase();
}
//# sourceMappingURL=normalizeSearchText.js.map