/*** Read and trim the first regex capture only when it satisfies configured length and whitespace constraints. */
export function readSafeRegexCapture(input, pattern, options = {}) {
    const value = pattern.exec(input)?.at(1)?.trim();
    if (!value)
        return undefined;
    if (value.length > (options.maxLength ?? 2048))
        return undefined;
    if (options.allowWhitespace !== true && /\s/u.test(value))
        return undefined;
    return value;
}
//# sourceMappingURL=readSafeRegexCapture.js.map