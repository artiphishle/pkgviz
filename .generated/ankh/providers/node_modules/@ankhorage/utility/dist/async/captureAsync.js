/*** Convert a promise into a ready/error loadable result while normalizing unknown failures to text. */
export async function captureAsync(operation, toMessage = String) {
    try {
        return { status: 'ready', data: await operation };
    }
    catch (error) {
        return { status: 'error', message: error instanceof Error ? error.message : toMessage(error) };
    }
}
//# sourceMappingURL=captureAsync.js.map