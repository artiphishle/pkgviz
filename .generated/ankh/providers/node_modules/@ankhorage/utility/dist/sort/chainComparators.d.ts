import type { Comparator } from './types.js';
/*** Compose comparators in priority order and return the first non-zero comparison result. */
export declare function chainComparators<TValue>(comparators: readonly Comparator<TValue>[]): Comparator<TValue>;
//# sourceMappingURL=chainComparators.d.ts.map