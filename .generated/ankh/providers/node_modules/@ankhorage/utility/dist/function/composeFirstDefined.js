/*** Compose resolvers and return the first defined result for an input. */
export function composeFirstDefined(resolvers) {
    return (input) => {
        for (const resolver of resolvers) {
            const result = resolver(input);
            if (result !== undefined)
                return result;
        }
        return undefined;
    };
}
//# sourceMappingURL=composeFirstDefined.js.map