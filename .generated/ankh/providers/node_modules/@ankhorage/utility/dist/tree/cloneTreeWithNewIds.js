/*** Deep-clone a tree while assigning a fresh id to every cloned node. */
export function cloneTreeWithNewIds(root, createId, adapter) {
    const clonedChildren = (adapter.getChildren(root) ?? []).map((child) => cloneTreeWithNewIds(child, createId, adapter));
    const withId = adapter.withId(root, createId(root));
    return adapter.withChildren(withId, clonedChildren);
}
//# sourceMappingURL=cloneTreeWithNewIds.js.map