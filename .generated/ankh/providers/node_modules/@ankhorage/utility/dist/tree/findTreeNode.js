/*** Find the first tree node with a matching id using depth-first traversal. */
export function findTreeNode(root, id, adapter) {
    if (Object.is(adapter.getId(root), id))
        return root;
    for (const child of adapter.getChildren(root) ?? []) {
        const found = findTreeNode(child, id, adapter);
        if (found !== undefined)
            return found;
    }
    return undefined;
}
//# sourceMappingURL=findTreeNode.js.map