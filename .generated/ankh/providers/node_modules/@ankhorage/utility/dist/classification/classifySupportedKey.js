import { readOwnProperty } from '../object/readOwnProperty.js';
/*** Classify a string key against a primary predicate and an explicit supported-key record. */
export function classifySupportedKey(key, isPrimary, explicitSupport = {}) {
    if (isPrimary(key))
        return 'primary';
    return readOwnProperty(explicitSupport, key) === true ? 'explicit' : 'unsupported';
}
//# sourceMappingURL=classifySupportedKey.js.map