/*** Open a URL in a new browser tab without opener access when an opener implementation is available. */
export function openUrl(url, opener) {
    if (!opener)
        return false;
    opener(url, '_blank', 'noopener,noreferrer');
    return true;
}
//# sourceMappingURL=openUrl.js.map