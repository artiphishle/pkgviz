/*** Normalize an HTTP(S) URL and reject embedded credentials or non-HTTP protocols. */
export function normalizeCredentialFreeHttpUrl(value) {
    const normalized = value.trim();
    if (normalized.length === 0)
        return null;
    try {
        const url = new URL(normalized);
        if (url.protocol !== 'http:' && url.protocol !== 'https:')
            return null;
        if (url.username.length > 0 || url.password.length > 0 || url.hostname.length === 0) {
            return null;
        }
        return url.toString();
    }
    catch {
        return null;
    }
}
//# sourceMappingURL=normalizeCredentialFreeHttpUrl.js.map