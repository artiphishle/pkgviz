/*** Resolve the highest-ranked severity from a diagnostics collection. */
export function highestSeverity(diagnostics, rank) {
    return diagnostics.reduce((highest, diagnostic) => {
        if (highest === undefined)
            return diagnostic.severity;
        return rank(diagnostic.severity) > rank(highest) ? diagnostic.severity : highest;
    }, undefined);
}
//# sourceMappingURL=highestSeverity.js.map