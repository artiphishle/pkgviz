import { isStringArray } from '@ankhorage/utility/array';
import { isNonEmptyString } from '@ankhorage/utility/string';
/*** Primitive field rules for portable Infra configuration, shared by its exact-shape validators. */
export const infraFields = {
    optionalText: (value) => value === undefined || isNonEmptyString(value),
    strings: (value) => isStringArray(value) && value.every(isNonEmptyString),
    optionalStrings: (value) => value === undefined || (isStringArray(value) && value.every(isNonEmptyString)),
    positiveInteger: (value) => typeof value === 'number' && Number.isSafeInteger(value) && value > 0,
    nonnegativeInteger: (value) => typeof value === 'number' && Number.isSafeInteger(value) && value >= 0,
    optionalPositiveInteger: (value) => value === undefined || (typeof value === 'number' && Number.isSafeInteger(value) && value > 0),
    optionalNonnegativeInteger: (value) => value === undefined || (typeof value === 'number' && Number.isSafeInteger(value) && value >= 0),
    port: (value) => typeof value === 'number' && Number.isInteger(value) && value > 0 && value <= 65535,
    optionalPort: (value) => value === undefined ||
        (typeof value === 'number' && Number.isInteger(value) && value > 0 && value <= 65535),
};
//# sourceMappingURL=infraFields.js.map