import { isInfraManifest } from './isInfraManifest';
/** Parse unknown infrastructure input; package availability and credentials are separate validation stages. */
export function parseInfraManifest(value) {
    return isInfraManifest(value)
        ? { ok: true, value, diagnostics: [] }
        : {
            ok: false,
            diagnostics: [
                {
                    severity: 'error',
                    code: 'invalid_manifest',
                    message: 'Expected canonical Infra environments with supported provider configuration and compute/runtime compatibility.',
                },
            ],
        };
}
//# sourceMappingURL=parseInfraManifest.js.map