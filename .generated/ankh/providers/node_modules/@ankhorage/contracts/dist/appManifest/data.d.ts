/*** Validate every endpoint in the data endpoint registry. */
export declare function isDataEndpointRegistry(value: unknown): boolean;
/*** Validate every schema in the data schema registry. */
export declare function isDataSchemaRegistry(value: unknown): boolean;
/*** Validate credential identity and optional descriptive metadata. */
export declare function isCredentialRef(value: unknown): boolean;
/*** Validate an adapter reference and its optional authored configuration. */
export declare function isAdapterRef(value: unknown): boolean;
//# sourceMappingURL=data.d.ts.map