import type { ColorSwatch, HexColor } from '@ankhorage/color-theory';
import type { RoleSemantics } from '../../../types/theme';
interface ResolveRoleSemanticsInput {
    base?: HexColor;
    disabledBg: HexColor;
    disabledText: HexColor;
    hover?: HexColor;
    id: string;
    isDark: boolean;
    measureForeground: (id: string, foreground: HexColor, background: HexColor, minimumContrast: number) => HexColor;
    onSurfaceText: HexColor;
    outline: HexColor;
    selectForeground: (id: string, background: HexColor, minimumContrast: number) => HexColor;
    swatch: ColorSwatch;
}
/** Resolve every filled role state together with its measured foreground. */
export declare function resolveRoleSemantics({ base, disabledBg, disabledText, hover, id, isDark, measureForeground, onSurfaceText, outline, selectForeground, swatch, }: ResolveRoleSemanticsInput): RoleSemantics;
export {};
//# sourceMappingURL=resolveRoleSemantics.d.ts.map