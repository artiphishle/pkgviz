const HEADING_KEYS = new Set(['1', '2', '3', '4', '5', '6']);
const HEADING_WEIGHTS = new Set(['regular', 'medium', 'semiBold', 'bold']);
export function resolveHeadingTokenOverrides(defaults, overrides) {
    validateHeadingKeys(overrides);
    return {
        1: resolveHeading('1', defaults[1], overrides?.['1']),
        2: resolveHeading('2', defaults[2], overrides?.['2']),
        3: resolveHeading('3', defaults[3], overrides?.['3']),
        4: resolveHeading('4', defaults[4], overrides?.['4']),
        5: resolveHeading('5', defaults[5], overrides?.['5']),
        6: resolveHeading('6', defaults[6], overrides?.['6']),
    };
}
function validateHeadingKeys(overrides) {
    for (const key of Object.keys(overrides ?? {})) {
        if (!HEADING_KEYS.has(key))
            throw new RangeError(`Unknown typography heading token: ${key}.`);
    }
}
function resolveHeading(key, fallback, override) {
    const size = override?.size ?? fallback.size;
    const lineHeight = override?.lineHeight ?? fallback.lineHeight;
    const weight = override?.weight ?? fallback.weight;
    if (!Number.isFinite(size) || size <= 0) {
        throw new RangeError(`Theme token typography.headings.${key}.size must be a finite positive number.`);
    }
    if (!Number.isFinite(lineHeight) || lineHeight <= 0) {
        throw new RangeError(`Theme token typography.headings.${key}.lineHeight must be a finite positive number.`);
    }
    if (!isHeadingWeight(weight)) {
        throw new RangeError(`Invalid heading weight for typography.headings.${key}: ${weight}.`);
    }
    return { size, lineHeight, weight };
}
function isHeadingWeight(value) {
    return HEADING_WEIGHTS.has(value);
}
//# sourceMappingURL=resolveHeadingTokenOverrides.js.map