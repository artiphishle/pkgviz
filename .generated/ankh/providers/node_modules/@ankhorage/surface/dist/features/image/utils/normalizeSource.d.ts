import type { ImageSourcePropType } from 'react-native';
import type { SurfaceImageSource } from '../../../types/image';
/*** Normalizes a Surface image source into React Native's source shape. */
export declare function normalizeSource(source: SurfaceImageSource | null | undefined): ImageSourcePropType | undefined;
//# sourceMappingURL=normalizeSource.d.ts.map