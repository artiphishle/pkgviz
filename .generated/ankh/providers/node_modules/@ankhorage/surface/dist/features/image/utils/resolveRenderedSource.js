/*** Chooses the primary or fallback normalized image source. */
export function resolveRenderedSource(normalizedPrimary, normalizedFallback, showFallback) {
    if (showFallback || !normalizedPrimary)
        return normalizedFallback;
    return normalizedPrimary;
}
//# sourceMappingURL=resolveRenderedSource.js.map