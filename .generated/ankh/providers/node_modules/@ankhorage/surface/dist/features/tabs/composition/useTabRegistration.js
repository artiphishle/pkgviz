import React from 'react';
import { useTabsContext } from './useTabsContext';
/*** Registers one focusable tab with the owning Tabs context. */
export function useTabRegistration({ disabled, value }) {
    const { registerTab, unregisterTab } = useTabsContext();
    const pressableRef = React.useRef(null);
    React.useEffect(() => {
        registerTab({
            disabled,
            focus: () => {
                const focusable = pressableRef.current;
                focusable?.focus?.();
            },
            value,
        });
        return () => unregisterTab(value);
    }, [disabled, registerTab, unregisterTab, value]);
    return pressableRef;
}
//# sourceMappingURL=useTabRegistration.js.map