import type { ToastController, ToastOptions } from '../../../types/toast';
/*** Owns the toast queue, dismissal timers, and imperative controller state. */
export declare function useToastRuntime(defaultDuration: number): {
    controller: ToastController;
    toasts: ToastEntry[];
};
interface ToastEntry extends ToastOptions {
    id: string;
}
export {};
//# sourceMappingURL=useToastRuntime.d.ts.map