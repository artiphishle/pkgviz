/*** Resolves the canonical resize mode, preferring the Surface fit alias. */
export function resolveResizeMode(fit, resizeMode) {
    return fit ?? resizeMode ?? 'cover';
}
//# sourceMappingURL=resolveResizeMode.js.map