import React from 'react';
import { resolveFieldState, resolveIndicatorSize, resolveSelectionControlColors, resolveSelectionControlNextChecked, } from '../../../../../internal/resolvers';
import { useControllableState } from '../../../../../internal/useControllableState';
import { View } from '../../../../layout/public';
import { Pressable } from '../../../../pressable/public';
import { useTheme } from '../../../../theme/runtime';
import { Text } from '../../../../typography/public';
/*** Renders a controlled or uncontrolled accessible checkbox. */
export function Checkbox({ accessibilityLabel, checked, children, color = 'primary', defaultChecked = false, disabled = false, invalid = false, onCheckedChange, readOnly = false, size = 'm', testID, ...buttonProps }) {
    const { theme } = useTheme();
    const [isChecked, setChecked] = useControllableState({
        value: checked,
        defaultValue: defaultChecked,
        onChange: onCheckedChange,
    });
    return (<CheckboxControl accessibilityLabel={accessibilityLabel} buttonProps={buttonProps} color={color} disabled={disabled} invalid={invalid} isChecked={isChecked} readOnly={readOnly} setChecked={setChecked} size={size} testID={testID} theme={theme}>
      {children}
    </CheckboxControl>);
}
/*** Owns the checkbox interaction boundary around resolved selection content. */
function CheckboxControl({ accessibilityLabel, buttonProps, children, color, disabled, invalid, isChecked, readOnly, setChecked, size, testID, theme, }) {
    const nextChecked = resolveSelectionControlNextChecked({
        checked: isChecked,
        disabled,
        kind: 'checkbox',
        readOnly,
    });
    return (<Pressable {...buttonProps} accessibilityLabel={accessibilityLabel} accessibilityRole="checkbox" accessibilityState={{ checked: isChecked }} disabled={disabled} onPress={nextChecked === null ? undefined : () => setChecked(nextChecked)} testID={testID}>
      {(interactionState) => renderCheckboxContent({
            children,
            color,
            disabled,
            interactionState,
            invalid,
            isChecked,
            readOnly,
            size,
            theme,
        })}
    </Pressable>);
}
/*** Renders the checkbox indicator and optional label for one interaction state. */
function renderCheckboxContent(input) {
    const { interactionState, theme } = input;
    const fieldState = resolveFieldState({
        disabled: input.disabled,
        focused: interactionState.focused,
        invalid: input.invalid,
        readOnly: input.readOnly,
    });
    const colors = resolveSelectionControlColors(theme, {
        checked: input.isChecked,
        fieldState,
        hovered: interactionState.hovered,
        pressed: interactionState.pressed,
        color: input.color,
    });
    const indicatorSize = resolveIndicatorSize(input.size);
    const labelEmphasis = colors.labelColor === theme.semantics.content.muted ? 'muted' : 'default';
    const indicatorColor = fieldState.invalid ? 'error' : input.color;
    return (<View style={resolveSelectionRowStyle(colors.opacity)}>
      <View radius="s" style={resolveCheckboxIndicatorStyle(colors.backgroundColor, colors.borderColor, indicatorSize.checkbox)}>
        {input.isChecked ? (<Text color={input.disabled ? undefined : indicatorColor} emphasis={input.disabled ? 'muted' : 'inverse'} variant="caption" weight="bold">
            ✓
          </Text>) : null}
      </View>
      {input.children ? (<View ml="s">
          <Text emphasis={labelEmphasis}>{input.children}</Text>
        </View>) : null}
    </View>);
}
/*** Resolves the horizontal selection-control row style. */
function resolveSelectionRowStyle(opacity) {
    return { alignItems: 'center', flexDirection: 'row', opacity };
}
/*** Resolves checkbox indicator dimensions and interaction colors. */
function resolveCheckboxIndicatorStyle(backgroundColor, borderColor, size) {
    return {
        alignItems: 'center',
        backgroundColor,
        borderColor,
        borderWidth: 1.5,
        height: size,
        justifyContent: 'center',
        width: size,
    };
}
//# sourceMappingURL=Checkbox.js.map