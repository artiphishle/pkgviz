import type { ImageStyle } from 'react-native';
import type { ImageProps } from '../../../types/image';
import type { SurfaceTheme } from '../../../types/theme';
/*** Resolves token-aware dimensions and radius for a Surface image. */
export declare function resolveImageStyle(theme: SurfaceTheme, props: Pick<ImageProps, 'width' | 'height' | 'aspectRatio' | 'radius'>): ImageStyle;
//# sourceMappingURL=resolveImageStyle.d.ts.map