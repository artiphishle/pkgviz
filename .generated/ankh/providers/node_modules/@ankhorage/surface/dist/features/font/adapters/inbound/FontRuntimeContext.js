import { createContext } from 'react';
const fallbackRuntime = {
    fontsLoaded: true,
    activeFontId: null,
    setActiveFontId: () => undefined,
};
/*** Holds Surface font runtime state for theme resolution. */
export const FontRuntimeContext = createContext(fallbackRuntime);
//# sourceMappingURL=FontRuntimeContext.js.map