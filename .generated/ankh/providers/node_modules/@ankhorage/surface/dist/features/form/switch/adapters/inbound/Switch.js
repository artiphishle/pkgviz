import React from 'react';
import { resolveFieldState, resolveIndicatorSize, resolveSelectionControlColors, resolveSelectionControlNextChecked, } from '../../../../../internal/resolvers';
import { useControllableState } from '../../../../../internal/useControllableState';
import { View } from '../../../../layout/public';
import { Pressable } from '../../../../pressable/public';
import { useTheme } from '../../../../theme/runtime';
import { Text } from '../../../../typography/public';
/*** Renders a controlled or uncontrolled accessible switch. */
export function Switch({ children, checked, defaultChecked = false, onCheckedChange, color = 'primary', size = 'm', disabled = false, invalid = false, readOnly = false, accessibilityLabel, testID, ...props }) {
    const { theme } = useTheme();
    const [isChecked, setChecked] = useControllableState({
        value: checked,
        defaultValue: defaultChecked,
        onChange: onCheckedChange,
    });
    const nextChecked = resolveSelectionControlNextChecked({
        checked: isChecked,
        disabled,
        kind: 'switch',
        readOnly,
    });
    return (<Pressable {...props} accessibilityLabel={accessibilityLabel} accessibilityRole="switch" accessibilityState={{ checked: isChecked }} disabled={disabled} onPress={nextChecked === null ? undefined : () => setChecked(nextChecked)} testID={testID}>
      {(interactionState) => renderSwitchContent({ children, color, disabled, invalid, isChecked, readOnly, size, theme }, interactionState)}
    </Pressable>);
}
/*** Resolves switch presentation for one interaction state and renders the visual control. */
function renderSwitchContent(input, interactionState) {
    const fieldState = resolveFieldState({
        disabled: input.disabled,
        focused: interactionState.focused,
        invalid: input.invalid,
        readOnly: input.readOnly,
    });
    const colors = resolveSelectionControlColors(input.theme, {
        checked: input.isChecked,
        fieldState,
        hovered: interactionState.hovered,
        pressed: interactionState.pressed,
        color: input.color,
    });
    const indicatorSize = resolveIndicatorSize(input.size);
    const labelEmphasis = colors.labelColor === input.theme.semantics.content.muted ? 'muted' : 'default';
    return (<View align="center" direction="row" opacity={colors.opacity}>
      <View bg={colors.trackColor} borderColor={colors.borderColor} borderWidth={1} height={indicatorSize.switchHeight} justify="center" minWidth={indicatorSize.switchWidth} px={2} radius="full" width={indicatorSize.switchWidth}>
        <View alignSelf={input.isChecked ? 'flex-end' : 'flex-start'} bg={colors.thumbColor} height={indicatorSize.switchThumb} radius="full" width={indicatorSize.switchThumb}/>
      </View>
      {input.children ? (<View ml="s">
          <Text emphasis={labelEmphasis}>{input.children}</Text>
        </View>) : null}
    </View>);
}
//# sourceMappingURL=Switch.js.map