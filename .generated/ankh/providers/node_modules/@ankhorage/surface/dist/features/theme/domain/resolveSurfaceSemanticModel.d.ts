import type { GeneratedThemeModeColors, HexColor, SemanticColorReferenceMap, SemanticColorToken, ThemeColorMode } from '@ankhorage/color-theory';
import type { SurfaceColorDiagnostics, ThemeSemantics } from '../../../types/theme';
type ResolvedSemanticColors = Record<SemanticColorToken, HexColor>;
interface ResolveSurfaceSemanticModelInput {
    generated: GeneratedThemeModeColors;
    mode: ThemeColorMode;
    references: SemanticColorReferenceMap;
    resolved: ResolvedSemanticColors;
}
/** Resolve the complete Surface-owned semantic model from canonical Color Theory output. */
export declare function resolveSurfaceSemanticModel(input: ResolveSurfaceSemanticModelInput): {
    semantics: ThemeSemantics;
    colorDiagnostics: SurfaceColorDiagnostics;
};
export {};
//# sourceMappingURL=resolveSurfaceSemanticModel.d.ts.map