import React from 'react';
import { type LayoutChangeEvent, type View } from 'react-native';
import type { PopoverPlacement } from '../../../../types/popover';
/*** Measures the anchor and resolves the current viewport-safe popover position. */
export declare function usePopoverLayout({ anchorRef, offset, open, placement }: UsePopoverLayoutInput): {
    measureAnchor: () => void;
    onContentLayout: (event: LayoutChangeEvent) => void;
    position: {
        left: number;
        top: number;
    } | undefined;
    ready: boolean;
};
interface UsePopoverLayoutInput {
    anchorRef: React.RefObject<View | null>;
    offset: number;
    open: boolean;
    placement: PopoverPlacement;
}
export {};
//# sourceMappingURL=usePopoverLayout.d.ts.map