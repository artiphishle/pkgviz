import type { TextStyle, ViewStyle } from 'react-native';
import type { TextInputProps } from '../../../../types/text-input';
import type { SurfaceTheme } from '../../../../types/theme';
/*** Resolves layout, colors, and editability for one TextInput state. */
export declare function resolveTextInputPresentation(theme: SurfaceTheme, props: TextInputProps, focused: boolean): TextInputPresentation;
interface TextInputPresentation {
    accessorySpacing: number;
    containerStyle: ViewStyle;
    editable: boolean;
    inputStyle: TextStyle;
    numberOfLines: number | undefined;
    placeholderColor: string;
}
export {};
//# sourceMappingURL=resolveTextInputPresentation.d.ts.map