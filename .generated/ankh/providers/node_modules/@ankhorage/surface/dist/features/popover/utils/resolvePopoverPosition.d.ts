import type { PopoverPlacement } from '../../../types/popover';
/*** Resolves a viewport-clamped anchored position for popover content. */
export declare function resolvePopoverPosition({ anchor, contentSize, offset, placement, viewport, viewportPadding, }: {
    anchor: {
        height: number;
        width: number;
        x: number;
        y: number;
    };
    contentSize: {
        height: number;
        width: number;
    };
    offset: number;
    placement: PopoverPlacement;
    viewport: {
        height: number;
        width: number;
    };
    viewportPadding: number;
}): {
    left: number;
    top: number;
};
//# sourceMappingURL=resolvePopoverPosition.d.ts.map