/*** Normalizes a Surface image source into React Native's source shape. */
export function normalizeSource(source) {
    if (source === null || source === undefined)
        return undefined;
    if (typeof source === 'string')
        return { uri: source };
    return source;
}
//# sourceMappingURL=normalizeSource.js.map