const WEIGHT_TOKEN_KEYS = new Set([
    'thin',
    'extraLight',
    'light',
    'regular',
    'medium',
    'semiBold',
    'bold',
    'extraBold',
    'black',
]);
const FONT_WEIGHT_VALUES = new Set([
    '100',
    '200',
    '300',
    '400',
    '500',
    '600',
    '700',
    '800',
    '900',
    'bold',
    'normal',
]);
export function resolveWeightTokenOverrides(overrides) {
    if (overrides === undefined)
        return {};
    return Object.fromEntries(Object.entries(overrides).map(([token, value]) => {
        if (!WEIGHT_TOKEN_KEYS.has(token)) {
            throw new RangeError(`Unknown typography weight token: ${token}.`);
        }
        if (!isFontWeight(value)) {
            throw new RangeError(`Invalid font weight for typography.weights.${token}: ${value}.`);
        }
        return [token, value];
    }));
}
function isFontWeight(value) {
    return FONT_WEIGHT_VALUES.has(value);
}
//# sourceMappingURL=resolveWeightTokenOverrides.js.map