import React from 'react';
import type { ButtonProps } from '../../../../types/button';
/*** Renders the primary Surface action control with semantic visual states. */
export declare function Button({ children, variant, color, size, disabled, loading, leadingIcon, trailingIcon, fullWidth, onPress, testID, ...props }: ButtonProps): React.JSX.Element;
//# sourceMappingURL=Button.d.ts.map