import type { ColorContrastContext, ColorSelectionTarget, ColorSwatch, ColorSwatchTiePolicy, HexColor, SemanticColorToken } from '@ankhorage/color-theory';
import type { ContentSemantics, NeutralSemantics, SurfaceContrastDiagnostic, SurfaceSemantics } from '../../../types/theme';
type ResolvedSemanticColors = Record<SemanticColorToken, HexColor>;
interface SurfaceSemanticMeasurements {
    measureForeground: (id: string, foreground: HexColor, background: HexColor, minimumContrast: number) => HexColor;
    measureSurface: (id: string, foreground: HexColor, background: HexColor, minimumContrast: number) => SurfaceContrastDiagnostic;
}
/** Create the canonical Surface policy for selecting and measuring semantic colors. */
export declare function createSurfaceSemanticPolicy(resolved: ResolvedSemanticColors, isDark: boolean, measurements: SurfaceSemanticMeasurements): SurfaceSemanticPolicy;
declare class SurfaceSemanticPolicy {
    private readonly resolved;
    private readonly isDark;
    private readonly measurements;
    readonly adjacentSurfaceContexts: readonly ColorContrastContext[];
    readonly textSurfaceContexts: readonly ColorContrastContext[];
    readonly tiePolicy: ColorSwatchTiePolicy;
    constructor(resolved: ResolvedSemanticColors, isDark: boolean, measurements: SurfaceSemanticMeasurements);
    buildNeutral(neutralSwatch: ColorSwatch): NeutralSemantics;
    buildSurface(inverse: HexColor): SurfaceSemantics;
    measureContent(content: ContentSemantics, surface: SurfaceSemantics): void;
    measureSurfaceSeparation(surface: SurfaceSemantics): SurfaceContrastDiagnostic[];
    neutralTarget(): ColorSelectionTarget;
    roleTarget(): ColorSelectionTarget;
    strongNeutralTarget(): ColorSelectionTarget;
    textTarget(): ColorSelectionTarget;
    private createSurfaceContexts;
    private measureSurface;
}
export {};
//# sourceMappingURL=createSurfaceSemanticPolicy.d.ts.map