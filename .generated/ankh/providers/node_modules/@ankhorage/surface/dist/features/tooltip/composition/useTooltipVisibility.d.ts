import React from 'react';
/*** Manages delayed tooltip visibility and timer cleanup. */
export declare function useTooltipVisibility(delay: number, passive: boolean): {
    readonly hide: () => void;
    readonly setVisible: React.Dispatch<React.SetStateAction<boolean>>;
    readonly show: () => void;
    readonly visible: boolean;
};
//# sourceMappingURL=useTooltipVisibility.d.ts.map