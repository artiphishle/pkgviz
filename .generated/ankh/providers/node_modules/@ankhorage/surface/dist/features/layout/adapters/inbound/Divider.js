import React from 'react';
import { View } from './View';
/*** Renders a horizontal or vertical separator using layout tokens. */
export function Divider({ orientation = 'horizontal', color = 'border', thickness = 1, ...props }) {
    return (<View {...props} bg={color} height={orientation === 'horizontal' ? thickness : '100%'} width={orientation === 'vertical' ? thickness : '100%'}/>);
}
//# sourceMappingURL=Divider.js.map