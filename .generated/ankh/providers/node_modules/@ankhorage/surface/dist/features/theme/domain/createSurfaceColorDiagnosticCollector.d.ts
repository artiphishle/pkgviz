import type { ColorContrastContext, ColorSelectionTarget, ColorSwatch, ColorSwatchTiePolicy, HexColor } from '@ankhorage/color-theory';
import type { SurfaceColorSelectionDiagnostic, SurfaceContrastDiagnostic } from '../../../types/theme';
/** Collect deterministic Color Theory evidence while Surface resolves semantic colors. */
export declare function createSurfaceColorDiagnosticCollector(): SurfaceColorDiagnosticCollector;
declare class SurfaceColorDiagnosticCollector {
    readonly selections: SurfaceColorSelectionDiagnostic[];
    readonly contrasts: SurfaceContrastDiagnostic[];
    readonly measureForeground: (id: string, foreground: HexColor, background: HexColor, minimumContrast: number) => HexColor;
    readonly measureSurface: (id: string, foreground: HexColor, background: HexColor, minimumContrast: number) => SurfaceContrastDiagnostic;
    readonly selectColor: (id: string, swatch: ColorSwatch, target: ColorSelectionTarget, contexts: readonly ColorContrastContext[], tiePolicy: ColorSwatchTiePolicy) => HexColor;
    readonly selectForeground: (id: string, background: HexColor, minimumContrast: number) => HexColor;
}
export {};
//# sourceMappingURL=createSurfaceColorDiagnosticCollector.d.ts.map