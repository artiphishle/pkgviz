import React from 'react';
import type { PopoverMenuAction } from '../../../types/popoverMenu';
interface PopoverMenuControllerOptions {
    actions: readonly PopoverMenuAction[];
    closeOnSelect: boolean;
    dismiss?: () => void;
    passive: boolean;
}
/*** Owns PopoverMenu visibility, active action state, and selection behavior. */
export declare function usePopoverMenuController({ actions, closeOnSelect, dismiss, passive, }: PopoverMenuControllerOptions): {
    readonly activeIndex: number;
    readonly activateAction: (action: PopoverMenuAction) => void;
    readonly close: () => void;
    readonly handleOpenChange: (nextOpen: boolean) => void;
    readonly open: boolean;
    readonly setActiveIndex: React.Dispatch<React.SetStateAction<number>>;
};
export {};
//# sourceMappingURL=usePopoverMenuController.d.ts.map