/*** Resolve a mapped theme token or return the supplied raw value unchanged. */
export function resolveToken(map, value) {
    if (value === undefined)
        return undefined;
    if (Object.prototype.hasOwnProperty.call(map, value)) {
        return map[value];
    }
    return value;
}
//# sourceMappingURL=resolveToken.js.map