export function resolveFieldState({ focused = false, disabled = false, invalid = false, readOnly = false, }) {
    let name = 'default';
    if (disabled) {
        name = 'disabled';
    }
    else if (invalid) {
        name = 'invalid';
    }
    else if (focused) {
        name = 'focused';
    }
    else if (readOnly) {
        name = 'readOnly';
    }
    return {
        name,
        focused,
        disabled,
        invalid,
        readOnly,
    };
}
//# sourceMappingURL=resolveFieldState.js.map