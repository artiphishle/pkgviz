import type { SurfaceColor } from '../../types/surfaceColor';
import type { SurfaceTheme } from '../../types/theme';
import type { FieldState } from './resolveFieldState';
export interface ResolvedSelectionControlColors {
    backgroundColor: string;
    borderColor: string;
    indicatorColor: string;
    labelColor: string;
    trackColor: string;
    thumbColor: string;
    opacity?: number;
}
export declare function resolveSelectionControlColors(theme: SurfaceTheme, { color, fieldState, checked, hovered, pressed, }: {
    color?: SurfaceColor;
    fieldState: FieldState;
    checked: boolean;
    hovered?: boolean;
    pressed?: boolean;
}): ResolvedSelectionControlColors;
//# sourceMappingURL=resolveSelectionControlColors.d.ts.map