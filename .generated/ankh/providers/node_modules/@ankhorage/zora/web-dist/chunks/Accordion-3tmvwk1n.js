'use client';
import {
  View3,
  Divider3
} from "./Accordion-ars389m1.js";
import {
  require_cjs,
  useTheme
} from "./Accordion-49gf1zb8.js";
import {
  IconButton2
} from "./Accordion-n2vxcm6e.js";
import {
  withZoraThemeScope
} from "./Accordion-grfbz6f2.js";
import {
  View2
} from "./Accordion-96cqeekv.js";
import {
  PopoverMenu2
} from "./Accordion-w9xmhjv6.js";
import {
  Heading2,
  Text2
} from "./Accordion-0exp2200.js";
import {
  useZoraTheme
} from "./Accordion-jftka3t5.js";
import {
  __toESM
} from "./Accordion-9qvmwkxe.js";

// node_modules/@ankhorage/surface/src/features/app-bar/adapters/inbound/AppBar.tsx
var import_react_native4 = __toESM(require_cjs(), 1);
import React4 from "react";

// node_modules/react-native-safe-area-context/lib/module/SafeAreaContext.js
var import_react_native2 = __toESM(require_cjs());
import * as React2 from "react";

// node_modules/react-native-safe-area-context/lib/module/NativeSafeAreaProvider.web.js
var import_react_native = __toESM(require_cjs());
import * as React from "react";

// node_modules/react-native-safe-area-context/lib/module/SafeAreaContext.js
var isDev = true;
var SafeAreaInsetsContext = /* @__PURE__ */ React2.createContext(null);
if (isDev) {
  SafeAreaInsetsContext.displayName = "SafeAreaInsetsContext";
}
var SafeAreaFrameContext = /* @__PURE__ */ React2.createContext(null);
if (isDev) {
  SafeAreaFrameContext.displayName = "SafeAreaFrameContext";
}
var styles = import_react_native2.StyleSheet.create({
  fill: {
    flex: 1
  }
});
var SafeAreaConsumer = SafeAreaInsetsContext.Consumer;
// node_modules/react-native-safe-area-context/lib/module/SafeAreaView.web.js
var import_react_native3 = __toESM(require_cjs());
import * as React3 from "react";

// node_modules/react-native-safe-area-context/lib/module/index.js

// node_modules/@ankhorage/surface/src/features/app-bar/adapters/inbound/AppBar.tsx
import { jsx, jsxs } from "react/jsx-runtime";
function AppBar({
  leading,
  trailing,
  children,
  safeAreaTop = true,
  divider = false,
  contentStyle,
  bg,
  style,
  ...props
}) {
  const { theme } = useTheme();
  const insets = React4.use(SafeAreaInsetsContext);
  const topInset = safeAreaTop ? insets?.top ?? 0 : 0;
  const safeAreaStyle = topInset === 0 ? undefined : { paddingTop: topInset };
  return /* @__PURE__ */ jsx(View3, {
    ...props,
    bg: bg ?? theme.semantics.surface.default,
    style,
    children: /* @__PURE__ */ jsxs(import_react_native4.View, {
      style: safeAreaStyle,
      children: [
        /* @__PURE__ */ jsxs(import_react_native4.View, {
          style: [styles2.content, resolveHorizontalPadding(theme.spacing.m), contentStyle],
          children: [
            leading ? /* @__PURE__ */ jsx(import_react_native4.View, {
              style: [styles2.accessory, resolveAccessoryMargin(theme.spacing.s, "leading")],
              children: leading
            }) : null,
            /* @__PURE__ */ jsx(import_react_native4.View, {
              style: styles2.center,
              children
            }),
            trailing ? /* @__PURE__ */ jsx(import_react_native4.View, {
              style: [styles2.accessory, resolveAccessoryMargin(theme.spacing.s, "trailing")],
              children: trailing
            }) : null
          ]
        }),
        divider ? /* @__PURE__ */ jsx(Divider3, {
          color: theme.semantics.neutral.divider,
          thickness: 1
        }) : null
      ]
    })
  });
}
function resolveHorizontalPadding(paddingHorizontal) {
  return { paddingHorizontal };
}
function resolveAccessoryMargin(spacing, position) {
  return position === "leading" ? { marginRight: spacing } : { marginLeft: spacing };
}
var styles2 = import_react_native4.StyleSheet.create({
  accessory: { flexShrink: 0 },
  center: { flex: 1, minWidth: 0 },
  content: { alignItems: "center", flexDirection: "row", minHeight: 56 }
});

// src/features/app-bar/adapters/inbound/AppBar.tsx
import { jsx as jsx2, jsxs as jsxs2 } from "react/jsx-runtime";
var DEFAULT_CANCEL_ICON = { name: "close-outline" };
var DEFAULT_OVERFLOW_ICON = { name: "ellipsis-vertical" };
var AppBar2 = withZoraThemeScope(AppBarInner);
function AppBarInner({
  themeId: _themeId,
  mode: _mode,
  title,
  subtitle,
  leading,
  actions,
  overflow,
  appMode,
  children,
  safeAreaTop = true,
  divider = true,
  testID,
  interactionPolicy
}) {
  const { theme } = useZoraTheme();
  const resolvedMode = resolveMode(appMode);
  const isSelectionMode = resolvedMode.type === "selection";
  const resolvedLeading = leading ?? resolveSelectionLeading(resolvedMode, interactionPolicy);
  const overflowMenu = resolveOverflowMenu(overflow, interactionPolicy, testID);
  const resolvedTrailing = resolveTrailing(actions, overflowMenu);
  const resolvedCenter = resolveCenter({
    children,
    isSelectionMode,
    resolvedMode,
    subtitle,
    title
  });
  return /* @__PURE__ */ jsx2(AppBar, {
    bg: isSelectionMode ? theme.semantics.action.primary.softBg : undefined,
    divider,
    leading: resolvedLeading,
    safeAreaTop,
    testID,
    trailing: resolvedTrailing,
    children: resolvedCenter ? /* @__PURE__ */ jsx2(View2, {
      style: { minWidth: 0 },
      children: resolvedCenter
    }) : null
  });
}
function resolveMode(mode) {
  return mode ?? { type: "default" };
}
function resolveSelectionLabel({ count, label }) {
  if (count === undefined)
    return label;
  return `${label} (${count})`;
}
function resolveOverflowLabel(overflow) {
  return overflow.label ?? "More options";
}
function resolveCancelLabel(mode) {
  return mode.cancelLabel ?? "Cancel selection";
}
function resolveSelectionLeading(mode, interactionPolicy) {
  if (mode.type !== "selection")
    return;
  return /* @__PURE__ */ jsx2(IconButton2, {
    color: "neutral",
    icon: mode.cancelIcon ?? DEFAULT_CANCEL_ICON,
    interactionPolicy,
    label: resolveCancelLabel(mode),
    onPress: mode.onCancel,
    size: "m",
    variant: "ghost"
  });
}
function resolveOverflowMenu(overflow, interactionPolicy, testID) {
  if (overflow === undefined || overflow.actions.length === 0)
    return null;
  return /* @__PURE__ */ jsx2(PopoverMenu2, {
    actions: overflow.actions,
    closeOnSelect: overflow.closeOnSelect,
    interactionPolicy,
    onDismiss: overflow.onDismiss,
    testID: testID ? `${testID}-overflow` : undefined,
    trigger: ({ toggle }) => /* @__PURE__ */ jsx2(IconButton2, {
      color: "neutral",
      disabled: overflow.disabled,
      icon: overflow.icon ?? DEFAULT_OVERFLOW_ICON,
      interactionPolicy,
      label: resolveOverflowLabel(overflow),
      onPress: toggle,
      size: "m",
      variant: "ghost"
    })
  });
}
function resolveTrailing(actions, overflowMenu) {
  if (!actions && !overflowMenu)
    return;
  return /* @__PURE__ */ jsxs2(View2, {
    direction: "row",
    align: "center",
    gap: "s",
    wrap: "nowrap",
    children: [
      actions,
      overflowMenu
    ]
  });
}
function resolveCenter({
  children,
  isSelectionMode,
  resolvedMode,
  subtitle,
  title
}) {
  if (children !== undefined)
    return children;
  if (isSelectionMode && resolvedMode.type === "selection") {
    return /* @__PURE__ */ jsx2(Text2, {
      emphasis: "default",
      numberOfLines: 1,
      variant: "label",
      weight: "semiBold",
      children: resolveSelectionLabel(resolvedMode)
    });
  }
  if (title == null && subtitle == null)
    return null;
  return /* @__PURE__ */ jsxs2(View2, {
    gap: "xs",
    children: [
      title != null ? /* @__PURE__ */ jsx2(Heading2, {
        ellipsizeMode: "tail",
        level: 3,
        numberOfLines: 1,
        size: "h5",
        children: title
      }) : null,
      subtitle != null ? /* @__PURE__ */ jsx2(Text2, {
        ellipsizeMode: "tail",
        emphasis: "muted",
        numberOfLines: 1,
        variant: "bodySmall",
        children: subtitle
      }) : null
    ]
  });
}
export { AppBar2 };
