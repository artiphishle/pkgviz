'use client';
import {
  View3
} from "./Accordion-ars389m1.js";
import {
  useTheme
} from "./Accordion-49gf1zb8.js";
import {
  Text3
} from "./Accordion-0jkm3y3w.js";
import {
  withZoraThemeScope
} from "./Accordion-grfbz6f2.js";
import {
  resolveControlSize,
  resolveButtonColors
} from "./Accordion-2d8xn32m.js";

// node_modules/@ankhorage/surface/src/features/badge/adapters/inbound/Badge.tsx
import { jsx } from "react/jsx-runtime";
function Badge({
  content,
  variant = "soft",
  color = "primary",
  size = "s",
  testID
}) {
  const { theme } = useTheme();
  const controlSize = resolveControlSize(theme, size);
  const colors = resolveButtonColors(theme, {
    variant,
    color,
    state: { disabled: false, focused: false, hovered: false, pressed: false }
  });
  return /* @__PURE__ */ jsx(View3, {
    px: Math.max(theme.spacing.xs, controlSize.paddingHorizontal - 4),
    py: Math.max(4, controlSize.paddingVertical - 2),
    radius: controlSize.borderRadius,
    style: resolveBadgeStyle(colors.backgroundColor, colors.borderColor, variant),
    testID,
    children: /* @__PURE__ */ jsx(Text3, {
      color,
      emphasis: variant === "solid" ? "inverse" : "default",
      variant: "bodySmall",
      weight: "medium",
      children: content
    })
  });
}
function resolveBadgeStyle(backgroundColor, borderColor, variant) {
  return {
    alignSelf: "flex-start",
    backgroundColor,
    borderColor,
    borderWidth: variant === "solid" ? 0 : 1
  };
}

// src/features/badge/utils/resolveBadgeRecipe.ts
function resolveBadgeRecipe({
  color = "primary",
  variant = "soft",
  size = "m"
}) {
  return { size, color, variant };
}

// src/features/badge/adapters/inbound/Badge.tsx
import { jsx as jsx2 } from "react/jsx-runtime";
var Badge2 = withZoraThemeScope(BadgeInner);
function BadgeInner({
  themeId: _themeId,
  mode: _mode,
  interactionPolicy: _interactionPolicy,
  children,
  color,
  variant,
  size,
  ...props
}) {
  const recipe = resolveBadgeRecipe({ color, variant, size });
  return /* @__PURE__ */ jsx2(Badge, {
    ...props,
    content: children,
    color: recipe.color,
    size: recipe.size,
    variant: recipe.variant
  });
}
export { resolveBadgeRecipe, Badge2 };
