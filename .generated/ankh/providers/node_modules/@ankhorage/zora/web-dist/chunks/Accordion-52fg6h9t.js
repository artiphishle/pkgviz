'use client';
// src/features/tree-view/utils/toggleExpandedIds.ts
function toggleExpandedIds(expandedIds, id) {
  return expandedIds.includes(id) ? expandedIds.filter((expandedId) => expandedId !== id) : [...expandedIds, id];
}

export { toggleExpandedIds };
