'use client';
// node_modules/@ankhorage/surface/src/features/theme/utils/resolveToken.ts
function resolveToken(map, value) {
  if (value === undefined)
    return;
  if (Object.prototype.hasOwnProperty.call(map, value)) {
    return map[value];
  }
  return value;
}

export { resolveToken };
