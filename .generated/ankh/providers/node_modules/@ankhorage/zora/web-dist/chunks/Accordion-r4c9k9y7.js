'use client';
import {
  View3
} from "./Accordion-ars389m1.js";
import {
  useTheme
} from "./Accordion-49gf1zb8.js";

// node_modules/@ankhorage/surface/src/features/surface/adapters/inbound/Surface.tsx
import { jsx } from "react/jsx-runtime";
function Surface3({ variant = "default", radius = "m", style, ...props }) {
  const { theme } = useTheme();
  const backgroundColor = variant === "subtle" ? theme.semantics.surface.subtle : variant === "raised" ? theme.semantics.surface.raised : theme.semantics.surface.default;
  const borderColor = theme.semantics.border.default;
  return /* @__PURE__ */ jsx(View3, {
    ...props,
    radius,
    style: [resolveSurfaceVariantStyles(variant, borderColor, backgroundColor), style]
  });
}
function resolveSurfaceVariantStyles(variant, borderColor, backgroundColor) {
  switch (variant) {
    case "subtle":
      return { backgroundColor, borderColor: "transparent", borderWidth: 0 };
    case "raised":
      return {
        backgroundColor,
        borderColor: "transparent",
        borderWidth: 0,
        elevation: 2,
        shadowColor: "#000000",
        shadowOpacity: 0.08,
        shadowOffset: { width: 0, height: 2 },
        shadowRadius: 8
      };
    case "outline":
      return { backgroundColor: "transparent", borderColor, borderWidth: 1 };
    case "default":
      return { backgroundColor, borderColor, borderWidth: 1 };
  }
}

export { Surface3 };
