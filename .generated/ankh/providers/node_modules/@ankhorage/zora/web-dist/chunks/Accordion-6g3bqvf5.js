'use client';
import {
  require_cjs
} from "./Accordion-49gf1zb8.js";
import {
  withZoraThemeScope
} from "./Accordion-grfbz6f2.js";
import {
  View2
} from "./Accordion-96cqeekv.js";
import {
  Text2
} from "./Accordion-0exp2200.js";
import {
  useZoraTheme
} from "./Accordion-jftka3t5.js";
import {
  __toESM
} from "./Accordion-9qvmwkxe.js";

// src/types/progress.ts
function resolveProgressRole(theme, color) {
  switch (color) {
    case "secondary":
      return theme.semantics.secondary;
    case "tertiary":
      return theme.semantics.accent;
    case "quaternary":
      return theme.semantics.highlight;
    case "error":
      return theme.semantics.error;
    case "info":
      return theme.semantics.info;
    case "primary":
      return theme.semantics.action.primary;
    case "danger":
      return theme.semantics.action.danger;
    case "success":
      return theme.semantics.success;
    case "warning":
      return theme.semantics.warning;
    case "neutral":
    default:
      return theme.semantics.action.neutral;
  }
}

// src/features/progress/utils/resolveProgressFraction.ts
function resolveProgressFraction({ value, max }) {
  if (!Number.isFinite(value) || !Number.isFinite(max) || max <= 0) {
    return 0;
  }
  const fraction = value / max;
  if (fraction <= 0) {
    return 0;
  }
  if (fraction >= 1) {
    return 1;
  }
  return fraction;
}

// src/features/progress/adapters/inbound/Progress.tsx
import { jsx } from "react/jsx-runtime";
function resolveProgressHeight(size) {
  switch (size) {
    case "s":
      return 4;
    case "m":
      return 6;
    case "l":
    default:
      return 8;
  }
}
function ProgressInner({
  themeId: _themeId,
  mode: _mode,
  interactionPolicy: _interactionPolicy,
  testID,
  value,
  max = 100,
  color = "primary",
  size = "m"
}) {
  const { theme } = useZoraTheme();
  const fraction = resolveProgressFraction({ value, max });
  const height = resolveProgressHeight(size);
  const role = resolveProgressRole(theme, color);
  return /* @__PURE__ */ jsx(View2, {
    accessibilityRole: "progressbar",
    bg: theme.semantics.neutral.surface,
    borderColor: theme.semantics.neutral.divider,
    borderWidth: 1,
    radius: "full",
    testID,
    style: { height, overflow: "hidden" },
    children: /* @__PURE__ */ jsx(View2, {
      bg: role.base,
      style: { height: "100%", width: `${fraction * 100}%` }
    })
  });
}
var Progress2 = withZoraThemeScope(ProgressInner);

// src/features/progress/adapters/inbound/ProgressRing.tsx
var import_react_native = __toESM(require_cjs(), 1);

// src/features/progress/utils/resolveProgressRingGeometry.ts
function resolveProgressRingGeometry({
  fraction,
  size,
  thickness
}) {
  const diameter = normalizePositiveNumber(size, DEFAULT_SIZE, MINIMUM_SIZE);
  const strokeWidth = Math.min(normalizePositiveNumber(thickness, DEFAULT_THICKNESS, MINIMUM_THICKNESS), diameter / 2);
  const centerlineRadius = (diameter - strokeWidth) / 2;
  const segmentCount = Math.max(MINIMUM_SEGMENT_COUNT, Math.min(MAXIMUM_SEGMENT_COUNT, Math.round(Math.PI * centerlineRadius)));
  const normalizedFraction = Math.min(Math.max(Number.isFinite(fraction) ? fraction : 0, 0), 1);
  return {
    diameter,
    filledSegmentCount: Math.round(normalizedFraction * segmentCount),
    segmentCount,
    segmentWidth: Math.max(1, 2 * Math.PI * centerlineRadius / segmentCount + 0.5),
    strokeWidth
  };
}
var DEFAULT_SIZE = 120;
var DEFAULT_THICKNESS = 10;
var MAXIMUM_SEGMENT_COUNT = 120;
var MINIMUM_SEGMENT_COUNT = 32;
var MINIMUM_SIZE = 24;
var MINIMUM_THICKNESS = 1;
function normalizePositiveNumber(value, fallback, minimum) {
  if (!Number.isFinite(value) || value <= 0) {
    return fallback;
  }
  return Math.max(value, minimum);
}

// src/features/progress/adapters/inbound/ProgressRing.tsx
import { jsx as jsx2, jsxs } from "react/jsx-runtime";
function ProgressRingInner({
  themeId: _themeId,
  mode: _mode,
  interactionPolicy: _interactionPolicy,
  accessibilityLabel = "Progress",
  accessibilityValueText,
  centerLabel,
  centerValue,
  color = "primary",
  max = 100,
  size = 120,
  testID,
  thickness = 10,
  trackColor = "neutral",
  value
}) {
  const { theme } = useZoraTheme();
  const fraction = resolveProgressFraction({ max, value });
  const geometry = resolveProgressRingGeometry({ fraction, size, thickness });
  const progressRole = resolveProgressRole(theme, color);
  const trackRole = resolveProgressRole(theme, trackColor);
  const accessibilityMaximum = Number.isFinite(max) && max > 0 ? max : 1;
  const accessibilityValue = fraction * accessibilityMaximum;
  const valueText = accessibilityValueText ?? `${Math.round(fraction * 100)}%`;
  const segments = Array.from({ length: geometry.filledSegmentCount });
  return /* @__PURE__ */ jsxs(import_react_native.View, {
    accessibilityLabel,
    accessibilityRole: "progressbar",
    accessibilityValue: {
      max: accessibilityMaximum,
      min: 0,
      now: accessibilityValue,
      text: valueText
    },
    accessible: true,
    style: { height: geometry.diameter, width: geometry.diameter },
    testID,
    children: [
      /* @__PURE__ */ jsxs(import_react_native.View, {
        importantForAccessibility: "no-hide-descendants",
        pointerEvents: "none",
        style: styles.visual,
        children: [
          /* @__PURE__ */ jsx2(View2, {
            style: [
              styles.track,
              {
                borderColor: trackRole.softBg,
                borderRadius: geometry.diameter / 2,
                borderWidth: geometry.strokeWidth
              }
            ]
          }),
          segments.map((_, index) => /* @__PURE__ */ jsx2(View2, {
            style: [
              styles.segmentLayer,
              { transform: [{ rotate: `${index * (360 / geometry.segmentCount)}deg` }] }
            ],
            children: /* @__PURE__ */ jsx2(View2, {
              style: {
                backgroundColor: progressRole.base,
                borderRadius: geometry.strokeWidth / 2,
                height: geometry.strokeWidth,
                left: (geometry.diameter - geometry.segmentWidth) / 2,
                position: "absolute",
                top: 0,
                width: geometry.segmentWidth
              }
            })
          }, index))
        ]
      }),
      centerValue !== undefined || centerLabel !== undefined ? /* @__PURE__ */ jsx2(import_react_native.View, {
        importantForAccessibility: "no-hide-descendants",
        pointerEvents: "none",
        style: styles.center,
        children: /* @__PURE__ */ jsxs(View2, {
          align: "center",
          gap: "xxs",
          px: "s",
          children: [
            centerValue !== undefined ? /* @__PURE__ */ jsx2(Text2, {
              align: "center",
              variant: "lead",
              weight: "bold",
              children: centerValue
            }) : null,
            centerLabel !== undefined ? /* @__PURE__ */ jsx2(Text2, {
              align: "center",
              emphasis: "muted",
              numberOfLines: 2,
              variant: "caption",
              children: centerLabel
            }) : null
          ]
        })
      }) : null
    ]
  });
}
var styles = import_react_native.StyleSheet.create({
  center: {
    alignItems: "center",
    bottom: 0,
    justifyContent: "center",
    left: 0,
    position: "absolute",
    right: 0,
    top: 0
  },
  segmentLayer: {
    bottom: 0,
    left: 0,
    position: "absolute",
    right: 0,
    top: 0
  },
  track: {
    bottom: 0,
    left: 0,
    position: "absolute",
    right: 0,
    top: 0
  },
  visual: {
    bottom: 0,
    left: 0,
    position: "absolute",
    right: 0,
    top: 0
  }
});
var ProgressRing2 = withZoraThemeScope(ProgressRingInner);
export { Progress2, ProgressRing2 };
