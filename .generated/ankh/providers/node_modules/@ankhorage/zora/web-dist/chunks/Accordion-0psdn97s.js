'use client';
import {
  useFocusManager
} from "./Accordion-6zb9d92y.js";
import {
  require_cjs
} from "./Accordion-49gf1zb8.js";
import {
  __toESM
} from "./Accordion-9qvmwkxe.js";

// node_modules/@ankhorage/surface/src/internal/focus/FocusScope.tsx
var import_react_native = __toESM(require_cjs(), 1);
import React from "react";
import { jsx } from "react/jsx-runtime";
function FocusScope({ active, children, onEscape, testID }) {
  const containerRef = React.useRef(null);
  const { bindKeydown, capturePreviousFocus, cycleFocus, focusFirst, restorePreviousFocus } = useFocusManager();
  React.useEffect(() => {
    if (!active) {
      return;
    }
    capturePreviousFocus();
    const timeoutId = setTimeout(() => {
      focusFirst(containerRef.current);
    }, 0);
    const unbind = bindKeydown((event) => {
      if (event.key === "Tab") {
        event.preventDefault();
        cycleFocus(containerRef.current, Boolean(event.shiftKey));
      }
      if (event.key === "Escape") {
        onEscape?.();
      }
    });
    return () => {
      clearTimeout(timeoutId);
      unbind();
      restorePreviousFocus();
    };
  }, [
    active,
    bindKeydown,
    capturePreviousFocus,
    cycleFocus,
    focusFirst,
    onEscape,
    restorePreviousFocus
  ]);
  return /* @__PURE__ */ jsx(import_react_native.View, {
    collapsable: false,
    ref: containerRef,
    testID,
    style: {
      flex: 1
    },
    children
  });
}

export { FocusScope };
