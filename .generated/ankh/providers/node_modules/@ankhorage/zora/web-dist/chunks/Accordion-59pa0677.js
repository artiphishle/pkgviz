'use client';
// node_modules/@ankhorage/utility/dist/object/isRecord.js
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// node_modules/@ankhorage/utility/dist/object/readOwnProperty.js
function readOwnProperty(target, key) {
  if (!Object.hasOwn(target, key))
    return;
  return Reflect.get(target, key);
}

// node_modules/@ankhorage/utility/dist/object/deepMerge.js
function deepMerge(target, source) {
  return mergeValue(target, source);
}
function mergeValue(target, source) {
  if (source === undefined)
    return target;
  if (!isRecord(target) || !isRecord(source))
    return source;
  return Object.fromEntries([
    ...Object.entries(target),
    ...Object.entries(source).filter(([, value]) => value !== undefined).map(([key, value]) => [key, mergeValue(readOwnProperty(target, key), value)])
  ]);
}
export { isRecord, deepMerge };
