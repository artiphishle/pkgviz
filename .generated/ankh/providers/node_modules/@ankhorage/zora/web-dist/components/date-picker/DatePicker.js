'use client';
import"../../chunks/Accordion-mnac2z83.js";
import"../../chunks/Accordion-99cj85ez.js";
import {
  Popover
} from "../../chunks/Accordion-cscmtcn8.js";
import"../../chunks/Accordion-jh7nxehr.js";
import"../../chunks/Accordion-ars389m1.js";
import"../../chunks/Accordion-49gf1zb8.js";
import"../../chunks/Accordion-r4c9k9y7.js";
import"../../chunks/Accordion-kcx57mk5.js";
import {
  withZoraThemeScope
} from "../../chunks/Accordion-grfbz6f2.js";
import {
  Surface2
} from "../../chunks/Accordion-h95wvyn5.js";
import"../../chunks/Accordion-t6sdgmbz.js";
import"../../chunks/Accordion-ftjkdjqs.js";
import"../../chunks/Accordion-0jkm3y3w.js";
import {
  Button2
} from "../../chunks/Accordion-n2vxcm6e.js";
import"../../chunks/Accordion-wn04ytqz.js";
import {
  View2
} from "../../chunks/Accordion-96cqeekv.js";
import {
  Text2
} from "../../chunks/Accordion-0exp2200.js";
import {
  Field3
} from "../../chunks/Accordion-395r3wra.js";
import"../../chunks/Accordion-61e19g3e.js";
import"../../chunks/Accordion-jftka3t5.js";
import"../../chunks/Accordion-ys214jt5.js";
import"../../chunks/Accordion-f5swy5bs.js";
import"../../chunks/Accordion-fre84yaq.js";

// src/features/date-picker/adapters/inbound/DatePicker.web.tsx
import React2 from "react";

// src/features/date-picker/composition/DatePickerContent.tsx
import React from "react";

// src/features/date-picker/utils/localDate.ts
var LOCAL_DATE_PATTERN = /^(\d{4})-(\d{2})-(\d{2})$/u;
function parseLocalDate(value) {
  if (!value)
    return;
  const match = LOCAL_DATE_PATTERN.exec(value);
  if (!match)
    return;
  const year = Number(match[1]);
  const month = Number(match[2]);
  const day = Number(match[3]);
  const parsed = new Date(year, month - 1, day);
  parsed.setFullYear(year);
  return parsed.getFullYear() === year && parsed.getMonth() === month - 1 && parsed.getDate() === day ? parsed : undefined;
}
function formatLocalDate(value) {
  const year = String(value.getFullYear()).padStart(4, "0");
  const month = String(value.getMonth() + 1).padStart(2, "0");
  const day = String(value.getDate()).padStart(2, "0");
  return `${year}-${month}-${day}`;
}

// src/features/date-picker/composition/DatePickerContent.tsx
import { jsx, jsxs } from "react/jsx-runtime";
var WEEKDAY_LABELS = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
var DAY_CELL_WIDTH = 42;
function DatePickerContent({
  description,
  interactionPolicy,
  label,
  maxDate,
  minDate,
  onDismiss,
  onSelect,
  testID,
  value
}) {
  const [displayMonth, setDisplayMonth] = React.useState(() => resolveInitialMonth(value, minDate));
  const monthDays = React.useMemo(() => createMonthDays(displayMonth), [displayMonth]);
  const resolvedMaxDate = React.useMemo(() => parseLocalDate(maxDate), [maxDate]);
  const resolvedMinDate = React.useMemo(() => parseLocalDate(minDate), [minDate]);
  return /* @__PURE__ */ jsxs(View2, {
    gap: "m",
    p: "m",
    testID: testID ? `${testID}-content` : undefined,
    children: [
      /* @__PURE__ */ jsx(PickerHeader, {
        description,
        title: label ?? "Choose date"
      }),
      /* @__PURE__ */ jsx(DatePickerMonthNavigation, {
        displayMonth,
        interactionPolicy,
        maxDate: resolvedMaxDate,
        minDate: resolvedMinDate,
        onMonthChange: setDisplayMonth
      }),
      /* @__PURE__ */ jsx(DatePickerWeekdays, {}),
      /* @__PURE__ */ jsx(DatePickerCalendar, {
        interactionPolicy,
        maxDate: resolvedMaxDate,
        minDate: resolvedMinDate,
        monthDays,
        onSelect,
        testID,
        value
      }),
      /* @__PURE__ */ jsx(Button2, {
        fullWidth: true,
        interactionPolicy,
        onPress: onDismiss,
        variant: "ghost",
        children: "Cancel"
      })
    ]
  });
}
function PickerHeader({
  description,
  title
}) {
  return /* @__PURE__ */ jsxs(View2, {
    gap: "xxs",
    children: [
      /* @__PURE__ */ jsx(Text2, {
        align: "center",
        variant: "label",
        weight: "semiBold",
        children: title
      }),
      description ? /* @__PURE__ */ jsx(Text2, {
        align: "center",
        emphasis: "muted",
        variant: "bodySmall",
        children: description
      }) : null
    ]
  });
}
function DatePickerMonthNavigation({
  displayMonth,
  interactionPolicy,
  maxDate,
  minDate,
  onMonthChange
}) {
  const previousMonth = new Date(displayMonth.getFullYear(), displayMonth.getMonth() - 1, 1);
  const nextMonth = new Date(displayMonth.getFullYear(), displayMonth.getMonth() + 1, 1);
  return /* @__PURE__ */ jsxs(View2, {
    align: "center",
    direction: "row",
    justify: "space-between",
    children: [
      /* @__PURE__ */ jsx(Button2, {
        disabled: !canNavigateToMonth(previousMonth, minDate, maxDate),
        interactionPolicy,
        onPress: () => onMonthChange(previousMonth),
        size: "s",
        variant: "ghost",
        children: "Previous"
      }),
      /* @__PURE__ */ jsx(Text2, {
        align: "center",
        variant: "label",
        weight: "semiBold",
        children: formatMonthLabel(displayMonth)
      }),
      /* @__PURE__ */ jsx(Button2, {
        disabled: !canNavigateToMonth(nextMonth, minDate, maxDate),
        interactionPolicy,
        onPress: () => onMonthChange(nextMonth),
        size: "s",
        variant: "ghost",
        children: "Next"
      })
    ]
  });
}
function DatePickerWeekdays() {
  return /* @__PURE__ */ jsx(View2, {
    direction: "row",
    justify: "space-between",
    children: WEEKDAY_LABELS.map((weekday) => /* @__PURE__ */ jsx(View2, {
      width: DAY_CELL_WIDTH,
      children: /* @__PURE__ */ jsx(Text2, {
        align: "center",
        emphasis: "muted",
        variant: "caption",
        weight: "semiBold",
        children: weekday
      })
    }, weekday))
  });
}
function DatePickerCalendar({
  interactionPolicy,
  maxDate,
  minDate,
  monthDays,
  onSelect,
  testID,
  value
}) {
  return /* @__PURE__ */ jsx(View2, {
    direction: "row",
    gap: "xs",
    wrap: "wrap",
    children: monthDays.map((day, index) => {
      if (!day)
        return /* @__PURE__ */ jsx(View2, {
          width: DAY_CELL_WIDTH
        }, `empty-${index}`);
      const selected = isSameLocalDay(value, day);
      return /* @__PURE__ */ jsx(View2, {
        width: DAY_CELL_WIDTH,
        children: /* @__PURE__ */ jsx(Button2, {
          color: selected ? "primary" : "neutral",
          disabled: isDateDisabled(day, minDate, maxDate),
          interactionPolicy,
          onPress: () => onSelect(day),
          size: "s",
          testID: testID ? `${testID}-day-${day.getDate()}` : undefined,
          variant: selected ? "solid" : "ghost",
          children: day.getDate()
        })
      }, day.toISOString());
    })
  });
}
function isSameLocalDay(left, right) {
  return left === formatLocalDate(right);
}
function startOfLocalDay(value) {
  return new Date(value.getFullYear(), value.getMonth(), value.getDate());
}
function isBeforeLocalDay(left, right) {
  return startOfLocalDay(left).getTime() < startOfLocalDay(right).getTime();
}
function isAfterLocalDay(left, right) {
  return startOfLocalDay(left).getTime() > startOfLocalDay(right).getTime();
}
function isDateDisabled(value, minDate, maxDate) {
  if (minDate && isBeforeLocalDay(value, minDate))
    return true;
  if (maxDate && isAfterLocalDay(value, maxDate))
    return true;
  return false;
}
function formatMonthLabel(value) {
  return value.toLocaleDateString(undefined, { month: "long", year: "numeric" });
}
function createMonthDays(month) {
  const firstDay = new Date(month.getFullYear(), month.getMonth(), 1);
  const dayCount = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate();
  const populatedLength = firstDay.getDay() + dayCount;
  const paddedLength = Math.ceil(populatedLength / 7) * 7;
  return Array.from({ length: paddedLength }, (_, index) => index < firstDay.getDay() ? null : new Date(month.getFullYear(), month.getMonth(), index - firstDay.getDay() + 1));
}
function resolveInitialMonth(value, minDate) {
  const base = parseLocalDate(value) ?? parseLocalDate(minDate) ?? new Date;
  return new Date(base.getFullYear(), base.getMonth(), 1);
}
function canNavigateToMonth(month, minDate, maxDate) {
  const firstDay = new Date(month.getFullYear(), month.getMonth(), 1);
  const lastDay = new Date(month.getFullYear(), month.getMonth() + 1, 0);
  if (maxDate && isAfterLocalDay(firstDay, maxDate))
    return false;
  if (minDate && isBeforeLocalDay(lastDay, minDate))
    return false;
  return true;
}

// src/features/date-picker/composition/DatePickerField.tsx
import { jsx as jsx2, jsxs as jsxs2 } from "react/jsx-runtime";
function DatePickerField({
  children,
  description,
  disabled,
  error,
  label,
  required,
  testID
}) {
  return /* @__PURE__ */ jsx2(Field3, {
    disabled,
    errorText: error,
    invalid: Boolean(error),
    label: label ? renderLabel(label, description) : undefined,
    required,
    testID,
    children
  });
}
function renderLabel(label, description) {
  return /* @__PURE__ */ jsxs2(View2, {
    gap: "xs",
    children: [
      /* @__PURE__ */ jsx2(Text2, {
        variant: "label",
        weight: "semiBold",
        children: label
      }),
      description ? /* @__PURE__ */ jsx2(Text2, {
        emphasis: "muted",
        variant: "bodySmall",
        children: description
      }) : null
    ]
  });
}

// src/features/date-picker/composition/DatePickerTrigger.tsx
import { jsx as jsx3 } from "react/jsx-runtime";
function DatePickerTrigger({
  disabled,
  formatDate,
  interactionPolicy,
  onPress,
  placeholder = "Choose date",
  testID,
  value
}) {
  const displayValue = value ? formatDate ? formatDate(value) : formatDefaultDate(value) : placeholder;
  return /* @__PURE__ */ jsx3(Button2, {
    disabled,
    interactionPolicy,
    onPress,
    testID: testID ? `${testID}-trigger` : undefined,
    trailingIcon: { name: "calendar-outline" },
    variant: "outline",
    children: displayValue
  });
}
function formatDefaultDate(value) {
  return parseLocalDate(value)?.toLocaleDateString() ?? value;
}

// src/features/date-picker/adapters/inbound/DatePicker.web.tsx
import { jsx as jsx4 } from "react/jsx-runtime";
var DatePicker = withZoraThemeScope(DatePickerInner);
function DatePickerInner({ themeId: _themeId, mode: _mode, ...props }) {
  const [open, setOpen] = React2.useState(false);
  return /* @__PURE__ */ jsx4(DatePickerField, {
    ...props,
    children: /* @__PURE__ */ jsx4(Popover, {
      anchor: ({ toggle }) => /* @__PURE__ */ jsx4(DatePickerTrigger, {
        ...props,
        onPress: toggle
      }),
      interactionPolicy: props.interactionPolicy,
      onOpenChange: setOpen,
      open,
      placement: "bottom-start",
      testID: props.testID ? `${props.testID}-popover` : undefined,
      children: /* @__PURE__ */ jsx4(Surface2, {
        variant: "raised",
        children: /* @__PURE__ */ jsx4(DatePickerContent, {
          description: props.description,
          interactionPolicy: props.interactionPolicy,
          label: props.label,
          maxDate: props.maxDate,
          minDate: props.minDate,
          onDismiss: () => setOpen(false),
          onSelect: (date) => {
            props.onValueChange?.(formatLocalDate(date));
            setOpen(false);
          },
          testID: props.testID,
          value: props.value
        })
      })
    })
  });
}
export {
  DatePicker
};
