'use client';
import"../../chunks/Accordion-mnac2z83.js";
import"../../chunks/Accordion-99cj85ez.js";
import {
  Popover
} from "../../chunks/Accordion-cscmtcn8.js";
import"../../chunks/Accordion-jh7nxehr.js";
import"../../chunks/Accordion-ars389m1.js";
import {
  require_cjs
} from "../../chunks/Accordion-49gf1zb8.js";
import"../../chunks/Accordion-r4c9k9y7.js";
import"../../chunks/Accordion-kcx57mk5.js";
import {
  withZoraThemeScope
} from "../../chunks/Accordion-grfbz6f2.js";
import {
  Surface2
} from "../../chunks/Accordion-h95wvyn5.js";
import"../../chunks/Accordion-t6sdgmbz.js";
import"../../chunks/Accordion-ftjkdjqs.js";
import"../../chunks/Accordion-0jkm3y3w.js";
import {
  Button2
} from "../../chunks/Accordion-n2vxcm6e.js";
import"../../chunks/Accordion-wn04ytqz.js";
import {
  View2
} from "../../chunks/Accordion-96cqeekv.js";
import {
  Text2
} from "../../chunks/Accordion-0exp2200.js";
import {
  Field3
} from "../../chunks/Accordion-395r3wra.js";
import"../../chunks/Accordion-61e19g3e.js";
import"../../chunks/Accordion-jftka3t5.js";
import"../../chunks/Accordion-ys214jt5.js";
import"../../chunks/Accordion-f5swy5bs.js";
import"../../chunks/Accordion-fre84yaq.js";
import {
  __toESM
} from "../../chunks/Accordion-9qvmwkxe.js";

// src/features/time-picker/adapters/inbound/TimePicker.web.tsx
import React2 from "react";

// src/features/time-picker/composition/TimePickerContent.tsx
var import_react_native = __toESM(require_cjs(), 1);
import React from "react";
import { jsx, jsxs } from "react/jsx-runtime";
var MINUTES_PER_DAY = 24 * 60;
var DEFAULT_STEP_MINUTES = 30;
function TimePickerContent({
  description,
  formatTime,
  interactionPolicy,
  label,
  maxTime,
  minTime,
  onDismiss,
  onSelect,
  stepMinutes,
  testID,
  value
}) {
  const options = React.useMemo(() => generateTimeOptions({ maxTime, minTime, stepMinutes }), [maxTime, minTime, stepMinutes]);
  return /* @__PURE__ */ jsxs(View2, {
    gap: "m",
    p: "m",
    testID: testID ? `${testID}-content` : undefined,
    children: [
      /* @__PURE__ */ jsx(PickerHeader, {
        description,
        title: label ?? "Choose time"
      }),
      /* @__PURE__ */ jsx(import_react_native.ScrollView, {
        style: styles.options,
        children: /* @__PURE__ */ jsx(View2, {
          gap: "xxs",
          children: options.map((option) => /* @__PURE__ */ jsx(TimePickerOption, {
            formatTime,
            interactionPolicy,
            onSelect,
            option,
            selected: option === value,
            testID
          }, option))
        })
      }),
      /* @__PURE__ */ jsx(Button2, {
        fullWidth: true,
        interactionPolicy,
        onPress: onDismiss,
        variant: "ghost",
        children: "Cancel"
      })
    ]
  });
}
function PickerHeader({
  description,
  title
}) {
  return /* @__PURE__ */ jsxs(View2, {
    gap: "xxs",
    children: [
      /* @__PURE__ */ jsx(Text2, {
        align: "center",
        variant: "label",
        weight: "semiBold",
        children: title
      }),
      description ? /* @__PURE__ */ jsx(Text2, {
        align: "center",
        emphasis: "muted",
        variant: "bodySmall",
        children: description
      }) : null
    ]
  });
}
function TimePickerOption({
  formatTime,
  interactionPolicy,
  onSelect,
  option,
  selected,
  testID
}) {
  return /* @__PURE__ */ jsx(Button2, {
    accessibilityState: { selected },
    fullWidth: true,
    interactionPolicy,
    onPress: () => onSelect(option),
    testID: testID ? `${testID}-option-${option}` : undefined,
    variant: selected ? "soft" : "ghost",
    children: formatTime ? formatTime(option) : option
  });
}
function parseTimeToMinutes(value) {
  if (!value)
    return;
  const match = /^(\d{2}):(\d{2})$/.exec(value);
  if (!match)
    return;
  const hours = Number(match[1]);
  const minutes = Number(match[2]);
  if (hours < 0 || hours > 23 || minutes < 0 || minutes > 59)
    return;
  return hours * 60 + minutes;
}
function formatMinutes(value) {
  const hours = Math.floor(value / 60);
  const minutes = value % 60;
  return `${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}`;
}
function resolveStepMinutes(stepMinutes) {
  if (stepMinutes === undefined || !Number.isFinite(stepMinutes) || stepMinutes <= 0) {
    return DEFAULT_STEP_MINUTES;
  }
  return Math.max(1, Math.floor(stepMinutes));
}
function generateTimeOptions({
  minTime,
  maxTime,
  stepMinutes
}) {
  const step = resolveStepMinutes(stepMinutes);
  const min = parseTimeToMinutes(minTime) ?? 0;
  const max = parseTimeToMinutes(maxTime) ?? MINUTES_PER_DAY - 1;
  return Array.from({ length: Math.ceil(MINUTES_PER_DAY / step) }, (_, index) => index * step).filter((minute) => minute >= min && minute <= max).map(formatMinutes);
}
var styles = import_react_native.StyleSheet.create({
  options: { maxHeight: 360 }
});

// src/features/time-picker/composition/TimePickerField.tsx
import { jsx as jsx2, jsxs as jsxs2 } from "react/jsx-runtime";
function TimePickerField({
  children,
  description,
  disabled,
  error,
  label,
  required,
  testID
}) {
  return /* @__PURE__ */ jsx2(Field3, {
    disabled,
    errorText: error,
    invalid: Boolean(error),
    label: label ? renderLabel(label, description) : undefined,
    required,
    testID,
    children
  });
}
function renderLabel(label, description) {
  return /* @__PURE__ */ jsxs2(View2, {
    gap: "xs",
    children: [
      /* @__PURE__ */ jsx2(Text2, {
        variant: "label",
        weight: "semiBold",
        children: label
      }),
      description ? /* @__PURE__ */ jsx2(Text2, {
        emphasis: "muted",
        variant: "bodySmall",
        children: description
      }) : null
    ]
  });
}

// src/features/time-picker/composition/TimePickerTrigger.tsx
import { jsx as jsx3 } from "react/jsx-runtime";
function TimePickerTrigger({
  disabled,
  formatTime,
  interactionPolicy,
  onPress,
  placeholder = "Choose time",
  testID,
  value
}) {
  const displayValue = value ? formatTime ? formatTime(value) : value : placeholder;
  return /* @__PURE__ */ jsx3(Button2, {
    disabled,
    interactionPolicy,
    onPress,
    testID: testID ? `${testID}-trigger` : undefined,
    trailingIcon: { name: "chevron-down-outline" },
    variant: "outline",
    children: displayValue
  });
}

// src/features/time-picker/adapters/inbound/TimePicker.web.tsx
import { jsx as jsx4 } from "react/jsx-runtime";
var TimePicker = withZoraThemeScope(TimePickerInner);
function TimePickerInner({ themeId: _themeId, mode: _mode, ...props }) {
  const [open, setOpen] = React2.useState(false);
  return /* @__PURE__ */ jsx4(TimePickerField, {
    ...props,
    children: /* @__PURE__ */ jsx4(Popover, {
      anchor: ({ toggle }) => /* @__PURE__ */ jsx4(TimePickerTrigger, {
        ...props,
        onPress: toggle
      }),
      interactionPolicy: props.interactionPolicy,
      onOpenChange: setOpen,
      open,
      placement: "bottom-start",
      testID: props.testID ? `${props.testID}-popover` : undefined,
      children: /* @__PURE__ */ jsx4(Surface2, {
        variant: "raised",
        children: /* @__PURE__ */ jsx4(TimePickerContent, {
          description: props.description,
          formatTime: props.formatTime,
          interactionPolicy: props.interactionPolicy,
          label: props.label,
          maxTime: props.maxTime,
          minTime: props.minTime,
          onDismiss: () => setOpen(false),
          onSelect: (time) => {
            props.onValueChange?.(time);
            setOpen(false);
          },
          stepMinutes: props.stepMinutes,
          testID: props.testID,
          value: props.value
        })
      })
    })
  });
}
export {
  TimePicker
};
