'use client';
import"../../chunks/Accordion-49gf1zb8.js";
import"../../chunks/Accordion-t6sdgmbz.js";
import"../../chunks/Accordion-jh7nxehr.js";
import"../../chunks/Accordion-ars389m1.js";
import"../../chunks/Accordion-ftjkdjqs.js";
import"../../chunks/Accordion-0jkm3y3w.js";
import {
  Button2
} from "../../chunks/Accordion-n2vxcm6e.js";
import"../../chunks/Accordion-kcx57mk5.js";
import {
  withZoraThemeScope
} from "../../chunks/Accordion-grfbz6f2.js";
import"../../chunks/Accordion-wn04ytqz.js";
import {
  View2
} from "../../chunks/Accordion-96cqeekv.js";
import {
  Icon2
} from "../../chunks/Accordion-paha5aje.js";
import {
  Text2
} from "../../chunks/Accordion-0exp2200.js";
import"../../chunks/Accordion-61e19g3e.js";
import {
  resolveIconSize
} from "../../chunks/Accordion-961ncx4e.js";
import {
  useZoraTheme
} from "../../chunks/Accordion-jftka3t5.js";
import"../../chunks/Accordion-ys214jt5.js";
import"../../chunks/Accordion-f5swy5bs.js";
import"../../chunks/Accordion-fre84yaq.js";

// src/features/breadcrumbs/adapters/inbound/Breadcrumbs.tsx
import React from "react";
import { jsx, jsxs, Fragment } from "react/jsx-runtime";
var Breadcrumbs = withZoraThemeScope(BreadcrumbsInner);
function BreadcrumbsInner({
  themeId: _themeId,
  mode: _mode,
  interactionPolicy,
  items,
  separator = "/",
  maxItems,
  compact = false,
  disabled = false,
  onItemPress,
  testID
}) {
  const normalizedMaxItems = normalizeMaxItems(maxItems);
  const renderItems = createRenderItems(items, normalizedMaxItems);
  const currentItemId = items.at(-1)?.id;
  if (items.length === 0)
    return null;
  return /* @__PURE__ */ jsx(View2, {
    direction: "row",
    align: "center",
    gap: compact ? "xs" : "s",
    testID,
    wrap: "wrap",
    children: renderItems.map((renderItem, index) => {
      const showSeparator = index !== renderItems.length - 1;
      if (renderItem.type === "ellipsis") {
        return /* @__PURE__ */ jsxs(React.Fragment, {
          children: [
            /* @__PURE__ */ jsx(Text2, {
              emphasis: "muted",
              variant: compact ? "caption" : "bodySmall",
              children: "…"
            }),
            showSeparator ? renderSeparator(separator) : null
          ]
        }, "ellipsis");
      }
      return /* @__PURE__ */ jsx(BreadcrumbItemView, {
        compact,
        current: renderItem.item.id === currentItemId,
        disabled,
        interactionPolicy,
        item: renderItem.item,
        onItemPress,
        separator: showSeparator ? separator : undefined,
        testID
      }, renderItem.item.id);
    })
  });
}
function BreadcrumbItemView({
  compact,
  current,
  disabled,
  interactionPolicy,
  item,
  onItemPress,
  separator,
  testID
}) {
  const interactive = !current && !disabled && !item.disabled && onItemPress !== undefined;
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      interactive ? /* @__PURE__ */ jsx(Button2, {
        color: "neutral",
        interactionPolicy,
        leadingIcon: item.icon,
        onPress: () => onItemPress({ id: item.id }),
        size: compact ? "s" : "m",
        testID: testID ? `${testID}-item-${item.id}` : undefined,
        variant: "ghost",
        children: item.label
      }) : /* @__PURE__ */ jsx(BreadcrumbLabel, {
        compact,
        item
      }),
      separator === undefined ? null : renderSeparator(separator)
    ]
  });
}
function BreadcrumbLabel({ item, compact }) {
  const { theme } = useZoraTheme();
  return /* @__PURE__ */ jsxs(View2, {
    direction: "row",
    align: "center",
    gap: "xs",
    wrap: "nowrap",
    children: [
      item.icon ? /* @__PURE__ */ jsx(Icon2, {
        ...item.icon,
        color: theme.semantics.content.muted,
        size: resolveIconSize(compact ? "s" : "m")
      }) : null,
      /* @__PURE__ */ jsx(Text2, {
        emphasis: "muted",
        variant: compact ? "caption" : "bodySmall",
        children: item.label
      })
    ]
  });
}
function renderSeparator(separator) {
  return /* @__PURE__ */ jsx(Text2, {
    emphasis: "muted",
    variant: "bodySmall",
    children: separator
  });
}
function normalizeMaxItems(value) {
  if (value === undefined || !Number.isFinite(value))
    return;
  return Math.max(0, Math.trunc(value));
}
function createRenderItems(items, maxItems) {
  if (maxItems === undefined || maxItems === 0 || items.length <= maxItems || maxItems < 3) {
    return items.map((item) => ({ item, type: "item" }));
  }
  const firstItem = items.at(0);
  if (firstItem === undefined)
    return [];
  const trailingCount = maxItems - 2;
  const trailingItems = items.slice(items.length - trailingCount);
  return [
    { item: firstItem, type: "item" },
    { type: "ellipsis" },
    ...trailingItems.map((item) => ({ item, type: "item" }))
  ];
}
export {
  Breadcrumbs
};
