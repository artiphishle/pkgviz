'use client';
import"../../chunks/Accordion-49gf1zb8.js";
import"../../chunks/Accordion-t6sdgmbz.js";
import"../../chunks/Accordion-kcx57mk5.js";
import {
  withZoraThemeScope
} from "../../chunks/Accordion-grfbz6f2.js";
import {
  Icon2
} from "../../chunks/Accordion-paha5aje.js";
import"../../chunks/Accordion-jh7nxehr.js";
import"../../chunks/Accordion-ars389m1.js";
import {
  View2
} from "../../chunks/Accordion-96cqeekv.js";
import {
  resolveIconSize
} from "../../chunks/Accordion-961ncx4e.js";
import {
  useZoraTheme
} from "../../chunks/Accordion-jftka3t5.js";
import"../../chunks/Accordion-f5swy5bs.js";
import"../../chunks/Accordion-fre84yaq.js";

// src/features/rating/utils/resolveRatingSegments.ts
function resolveRatingSegments({ value, max }) {
  const normalizedMax = Math.max(0, Math.trunc(Number.isFinite(max) ? max : 0));
  const normalizedValue = Math.min(Math.max(Number.isFinite(value) ? value : 0, 0), normalizedMax);
  return Array.from({ length: normalizedMax }, (_, index) => {
    const remaining = normalizedValue - index;
    if (remaining >= 1)
      return "full";
    if (remaining >= 0.5)
      return "half";
    return "empty";
  });
}

// src/features/rating/adapters/inbound/Rating.tsx
import { jsx } from "react/jsx-runtime";
var Rating = withZoraThemeScope(RatingInner);
function RatingInner({
  themeId: _themeId,
  mode: _mode,
  interactionPolicy: _interactionPolicy,
  testID,
  value,
  max = 5,
  color = "warning",
  size = "m"
}) {
  const { theme } = useZoraTheme();
  const role = resolveRatingRole(theme, color);
  const segments = resolveRatingSegments({ value, max });
  const iconSize = resolveIconSize(size);
  return /* @__PURE__ */ jsx(View2, {
    direction: "row",
    align: "center",
    gap: "xxs",
    testID,
    wrap: "nowrap",
    children: segments.map((segment, index) => {
      const name = segment === "full" ? "star" : segment === "half" ? "star-half" : "star-outline";
      const segmentColor = segment === "empty" ? theme.semantics.content.muted : role.base;
      return /* @__PURE__ */ jsx(Icon2, {
        color: segmentColor,
        name,
        size: iconSize
      }, `${index}-${segment}`);
    })
  });
}
function resolveRatingRole(theme, color) {
  switch (color) {
    case "secondary":
      return theme.semantics.secondary;
    case "tertiary":
      return theme.semantics.accent;
    case "quaternary":
      return theme.semantics.highlight;
    case "error":
      return theme.semantics.error;
    case "info":
      return theme.semantics.info;
    case "primary":
      return theme.semantics.action.primary;
    case "danger":
      return theme.semantics.action.danger;
    case "success":
      return theme.semantics.success;
    case "warning":
      return theme.semantics.warning;
    case "neutral":
    default:
      return theme.semantics.action.neutral;
  }
}
export {
  Rating
};
