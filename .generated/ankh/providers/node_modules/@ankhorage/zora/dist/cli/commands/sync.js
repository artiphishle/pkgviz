import { resolve } from 'node:path';
import { synchronizeDeclaredWebComponentsWithNodeAsync } from '../../features/web-component-artifact/composition/synchronizeDeclaredWebComponentsWithNodeAsync';
/*** Reconcile the declared web component set from the installed ZORA provider. */
export async function sync(request) {
    try {
        if (request.argv.length !== 1 || request.argv[0] !== '--web') {
            throw new Error('Usage: ankh zora sync --web');
        }
        const result = await synchronizeDeclaredWebComponentsWithNodeAsync(resolve(request.context.cwd));
        request.context.writeStdout(`Synchronized ${result.components.length} ZORA web component(s) at ${result.outputDirectory}\n`);
        return { exitCode: 0 };
    }
    catch (error) {
        request.context.writeStderr(`ZORA sync failed: ${error instanceof Error ? error.message : String(error)}\n`);
        return { exitCode: 1 };
    }
}
//# sourceMappingURL=sync.js.map