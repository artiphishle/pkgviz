import type { ManifestListSection } from '../../../../types/manifest-list';
/*** Partitions ordered list children into declared sections while enforcing complete coverage and identity. */
export declare function partitionListSections<T>(items: readonly T[], sections: readonly ManifestListSection[]): {
    data: T[];
    key: string;
    title?: string;
    itemCount: number;
}[];
//# sourceMappingURL=partitionListSections.d.ts.map