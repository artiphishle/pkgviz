import { Button } from '../../../button/public';
const SELECT_CHEVRON_ICON = { name: 'chevron-down' };
/*** Renders the shared Select trigger with the resolved option label or placeholder. */
export function SelectTrigger({ displayLabel, onPress, props, }) {
    return (<Button color={props.invalid ? 'danger' : 'neutral'} disabled={props.disabled} fullWidth interactionPolicy={props.interactionPolicy} onPress={props.readOnly ? undefined : onPress} testID={props.testID ? `${props.testID}-trigger` : undefined} trailingIcon={SELECT_CHEVRON_ICON} variant="outline">
      {displayLabel ?? props.placeholder ?? 'Select'}
    </Button>);
}
//# sourceMappingURL=SelectTrigger.js.map