import type React from 'react';
import type { TimePickerProps } from '../../../types/time-picker';
/*** Renders the shared TimePicker field frame around a platform-specific trigger. */
export declare function TimePickerField({ children, description, disabled, error, label, required, testID, }: TimePickerFieldProps): React.JSX.Element;
interface TimePickerFieldProps extends Pick<TimePickerProps, 'description' | 'disabled' | 'error' | 'label' | 'required' | 'testID'> {
    children: React.ReactNode;
}
export {};
//# sourceMappingURL=TimePickerField.d.ts.map