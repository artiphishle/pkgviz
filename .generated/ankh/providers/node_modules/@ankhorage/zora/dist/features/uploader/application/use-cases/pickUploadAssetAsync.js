/*** Selects the appropriate picker capability for the requested upload type. */
export async function pickUploadAssetAsync(input, imageOrVideoPicker, documentOrFilePicker) {
    const picker = input.type === 'image' || input.type === 'video' ? imageOrVideoPicker : documentOrFilePicker;
    return picker.pickAsync(input);
}
//# sourceMappingURL=pickUploadAssetAsync.js.map