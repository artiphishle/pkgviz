import type React from 'react';
import type { DatePickerProps } from '../../../types/date-picker';
/*** Renders the shared DatePicker trigger button for native and web hosts. */
export declare function DatePickerTrigger({ disabled, formatDate, interactionPolicy, onPress, placeholder, testID, value, }: DatePickerTriggerProps): React.JSX.Element;
interface DatePickerTriggerProps extends Pick<DatePickerProps, 'disabled' | 'formatDate' | 'interactionPolicy' | 'placeholder' | 'testID' | 'value'> {
    onPress: () => void;
}
export {};
//# sourceMappingURL=DatePickerTrigger.d.ts.map