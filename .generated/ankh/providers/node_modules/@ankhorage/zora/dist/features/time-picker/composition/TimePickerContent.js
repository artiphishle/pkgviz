import React from 'react';
import { ScrollView, StyleSheet } from 'react-native';
import { Button } from '../../button/public';
import { View } from '../../layout/public';
import { Text } from '../../typography/public';
const MINUTES_PER_DAY = 24 * 60;
const DEFAULT_STEP_MINUTES = 30;
/*** Renders shared time options for native BottomSheet and web Popover hosts. */
export function TimePickerContent({ description, formatTime, interactionPolicy, label, maxTime, minTime, onDismiss, onSelect, stepMinutes, testID, value, }) {
    const options = React.useMemo(() => generateTimeOptions({ maxTime, minTime, stepMinutes }), [maxTime, minTime, stepMinutes]);
    return (<View gap="m" p="m" testID={testID ? `${testID}-content` : undefined}>
      <PickerHeader description={description} title={label ?? 'Choose time'}/>
      <ScrollView style={styles.options}>
        <View gap="xxs">
          {options.map((option) => (<TimePickerOption formatTime={formatTime} interactionPolicy={interactionPolicy} key={option} onSelect={onSelect} option={option} selected={option === value} testID={testID}/>))}
        </View>
      </ScrollView>
      <Button fullWidth interactionPolicy={interactionPolicy} onPress={onDismiss} variant="ghost">
        Cancel
      </Button>
    </View>);
}
/*** Renders the shared title block used by picker overlays. */
function PickerHeader({ description, title, }) {
    return (<View gap="xxs">
      <Text align="center" variant="label" weight="semiBold">
        {title}
      </Text>
      {description ? (<Text align="center" emphasis="muted" variant="bodySmall">
          {description}
        </Text>) : null}
    </View>);
}
/*** Renders one accessible selectable time option. */
function TimePickerOption({ formatTime, interactionPolicy, onSelect, option, selected, testID, }) {
    return (<Button accessibilityState={{ selected }} fullWidth interactionPolicy={interactionPolicy} onPress={() => onSelect(option)} testID={testID ? `${testID}-option-${option}` : undefined} variant={selected ? 'soft' : 'ghost'}>
      {formatTime ? formatTime(option) : option}
    </Button>);
}
/*** Parses one HH:mm value to minutes since midnight. */
function parseTimeToMinutes(value) {
    if (!value)
        return undefined;
    const match = /^(\d{2}):(\d{2})$/.exec(value);
    if (!match)
        return undefined;
    const hours = Number(match[1]);
    const minutes = Number(match[2]);
    if (hours < 0 || hours > 23 || minutes < 0 || minutes > 59)
        return undefined;
    return hours * 60 + minutes;
}
/*** Formats minutes since midnight as HH:mm. */
function formatMinutes(value) {
    const hours = Math.floor(value / 60);
    const minutes = value % 60;
    return `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}`;
}
/*** Normalizes the configured time step to a positive whole minute. */
function resolveStepMinutes(stepMinutes) {
    if (stepMinutes === undefined || !Number.isFinite(stepMinutes) || stepMinutes <= 0) {
        return DEFAULT_STEP_MINUTES;
    }
    return Math.max(1, Math.floor(stepMinutes));
}
/*** Generates selectable HH:mm values within the configured range. */
function generateTimeOptions({ minTime, maxTime, stepMinutes, }) {
    const step = resolveStepMinutes(stepMinutes);
    const min = parseTimeToMinutes(minTime) ?? 0;
    const max = parseTimeToMinutes(maxTime) ?? MINUTES_PER_DAY - 1;
    return Array.from({ length: Math.ceil(MINUTES_PER_DAY / step) }, (_, index) => index * step)
        .filter((minute) => minute >= min && minute <= max)
        .map(formatMinutes);
}
const styles = StyleSheet.create({
    options: { maxHeight: 360 },
});
//# sourceMappingURL=TimePickerContent.js.map