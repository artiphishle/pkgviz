import { Field } from '@ankhorage/surface';
/*** Wraps Select presentation in the canonical Surface form-field semantics. */
export function SelectField({ children, props }) {
    return (<Field disabled={props.disabled} errorText={props.errorText} helperText={props.helperText} invalid={props.invalid} label={props.label} readOnly={props.readOnly} required={props.required} testID={props.testID ? `${props.testID}-field` : undefined}>
      {children}
    </Field>);
}
//# sourceMappingURL=SelectField.js.map