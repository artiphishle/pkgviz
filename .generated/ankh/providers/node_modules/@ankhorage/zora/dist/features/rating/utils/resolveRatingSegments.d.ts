export type RatingSegment = 'empty' | 'full' | 'half';
/*** Resolves a numeric rating into full, half, and empty visual segments. */
export declare function resolveRatingSegments({ value, max }: ResolveRatingSegmentsInput): RatingSegment[];
interface ResolveRatingSegmentsInput {
    value: number;
    max: number;
}
export {};
//# sourceMappingURL=resolveRatingSegments.d.ts.map