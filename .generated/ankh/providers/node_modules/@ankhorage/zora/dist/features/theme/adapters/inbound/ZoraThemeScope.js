import { ThemeScope } from '@ankhorage/surface';
import React, { useMemo } from 'react';
import { useZoraThemeRuntime, ZoraThemeRuntimeContext, } from '../../composition/ZoraThemeRuntimeContext';
import { resolveZoraScopedThemeId } from '../../utils/resolveZoraScopedThemeId';
/*** Applies nested ZORA theme overrides through the public Surface theme scope. */
export function ZoraThemeScope({ children, themeId, mode }) {
    if (mode === undefined && themeId === undefined)
        return children;
    return (<ZoraThemeScopeInner mode={mode} themeId={themeId}>
      {children}
    </ZoraThemeScopeInner>);
}
/*** Resolves and provides the scoped Surface and ZORA theme runtime values. */
function ZoraThemeScopeInner({ children, themeId, mode }) {
    const parentRuntime = useZoraThemeRuntime();
    const scopedThemeId = resolveZoraScopedThemeId({
        desiredThemeId: themeId,
        inheritedThemeId: parentRuntime.themeId,
    });
    const scopedRuntimeValue = useMemo(() => ({ themeId: scopedThemeId }), [scopedThemeId]);
    const scopedChildren = mode === undefined ? children : <ThemeScope mode={mode}>{children}</ThemeScope>;
    return (<ZoraThemeRuntimeContext value={scopedRuntimeValue}>{scopedChildren}</ZoraThemeRuntimeContext>);
}
//# sourceMappingURL=ZoraThemeScope.js.map