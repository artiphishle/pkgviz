export function resolveDialogWidth(width = 'default') {
    switch (width) {
        case 'narrow':
            return 420;
        case 'wide':
            return 560;
        case 'default':
        default:
            return 520;
    }
}
//# sourceMappingURL=resolveDialogWidth.js.map