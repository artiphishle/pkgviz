/*** Normalizes pagination input and resolves visible page and ellipsis items. */
export declare function resolvePaginationState({ page, pageCount, siblingCount, boundaryCount, compact, }: PaginationStateInput): PaginationState;
type PaginationItem = number | 'ellipsis';
interface PaginationStateInput {
    page: number;
    pageCount: number;
    siblingCount: number | undefined;
    boundaryCount: number | undefined;
    compact: boolean;
}
interface PaginationState {
    currentPage: number;
    pageCount: number;
    items: readonly PaginationItem[];
}
export {};
//# sourceMappingURL=resolvePaginationState.d.ts.map