import { createNodeWebComponentArtifactFileSystem } from '../adapters/outbound/node/createNodeWebComponentArtifactFileSystem';
import { synchronizeWebComponentArtifactsAsync, } from '../application/use-cases/synchronizeWebComponentArtifactsAsync';
/*** Wire Node filesystem operations into shared ZORA web reconciliation. */
export async function synchronizeWebComponentArtifactsWithNodeAsync(input) {
    return await synchronizeWebComponentArtifactsAsync(input, createNodeWebComponentArtifactFileSystem());
}
//# sourceMappingURL=synchronizeWebComponentArtifactsWithNodeAsync.js.map