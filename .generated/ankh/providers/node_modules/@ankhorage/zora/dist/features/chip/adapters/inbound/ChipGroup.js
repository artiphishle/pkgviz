import React from 'react';
import { View } from '../../../layout/public';
import { withZoraThemeScope } from '../../../theme/adapters/inbound/withZoraThemeScope';
import { Chip } from '../../public';
/***
 * Renders a row or wrap layout of `Chip` items.
 */
export const ChipGroup = withZoraThemeScope(ChipGroupInner);
function hasValue(values, value) {
    return values.includes(value);
}
function toggleValue(values, value) {
    return hasValue(values, value) ? values.filter((item) => item !== value) : [...values, value];
}
function ChipGroupInner({ themeId: _themeId, mode: _mode, testID, items, value, onValueChange, multiple, color = 'neutral', size = 's', wrap = true, disabled, interactionPolicy, }) {
    const passive = interactionPolicy === 'passive';
    const renderChip = (item) => {
        const itemDisabled = disabled === true || item.disabled === true;
        const isSelected = Array.isArray(value) ? hasValue(value, item.value) : value === item.value;
        const handlePress = () => {
            if (passive)
                return;
            if (multiple) {
                const next = toggleValue(value ?? [], item.value);
                onValueChange?.(next);
                return;
            }
            onValueChange?.(item.value);
        };
        return (<Chip interactionPolicy={interactionPolicy} key={item.value} disabled={itemDisabled} icon={item.icon} onPress={handlePress} selected={isSelected} size={size} testID={item.testID} color={color}>
        {item.label}
      </Chip>);
    };
    return (<View direction="row" align="center" gap="s" testID={testID} wrap={wrap ? 'wrap' : 'nowrap'}>
      {items.map(renderChip)}
    </View>);
}
//# sourceMappingURL=ChipGroup.js.map