/*** Resolves a table row's identity from its declared field or explicit React callback. */
export function resolveDataTableRowKey(row, index, rowKey, rowId) {
    if (rowId)
        return rowId(row, index);
    const key = rowKey === undefined ? undefined : new Map(Object.entries(row)).get(String(rowKey));
    if (typeof key === 'string' || typeof key === 'number')
        return String(key);
    throw new Error('DataTable requires a rowKey field containing a string or number for every row.');
}
//# sourceMappingURL=resolveDataTableRowKey.js.map