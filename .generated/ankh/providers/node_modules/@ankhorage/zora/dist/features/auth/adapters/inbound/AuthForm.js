import React from 'react';
import { FormActions, FormError, useFormController } from '../../../form/public';
import { View } from '../../../layout/public';
import { AuthFormField } from './AuthFormField';
/*** Keeps config-driven field orchestration private to the auth patterns that own it. */
export function AuthForm({ fields, values, onChange, onSubmit, errors, error, loading = false, disabled = false, submitLabel = 'Submit', actions, testID, interactionPolicy, }) {
    const controller = useFormController({ fields, values, errors, onChange, onSubmit });
    return (<View gap="m" testID={testID}>
      <FormError error={error}/>
      {fields.map((field) => (<AuthFormField key={field.name} disabled={disabled} error={controller.errors[field.name]} field={field} interactionPolicy={interactionPolicy} loading={loading} onChange={controller.setFieldValue} value={values[field.name]}/>))}
      <FormActions disabled={disabled} interactionPolicy={interactionPolicy} loading={loading} onSubmit={() => {
            if (interactionPolicy !== 'passive')
                void controller.handleSubmit();
        }} submitLabel={submitLabel}>
        {actions}
      </FormActions>
    </View>);
}
//# sourceMappingURL=AuthForm.js.map