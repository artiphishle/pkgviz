import { Field } from '@ankhorage/surface';
import { View } from '../../layout/public';
import { Text } from '../../typography/public';
/*** Renders the shared DatePicker field frame around a platform-specific trigger. */
export function DatePickerField({ children, description, disabled, error, label, required, testID, }) {
    return (<Field disabled={disabled} errorText={error} invalid={Boolean(error)} label={label ? renderLabel(label, description) : undefined} required={required} testID={testID}>
      {children}
    </Field>);
}
/*** Renders the picker label and optional supporting description. */
function renderLabel(label, description) {
    return (<View gap="xs">
      <Text variant="label" weight="semiBold">
        {label}
      </Text>
      {description ? (<Text emphasis="muted" variant="bodySmall">
          {description}
        </Text>) : null}
    </View>);
}
//# sourceMappingURL=DatePickerField.js.map