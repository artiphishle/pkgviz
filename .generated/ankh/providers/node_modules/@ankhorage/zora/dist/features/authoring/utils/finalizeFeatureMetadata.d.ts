import type { ZoraComponentMetaRegistry } from '../../../types/authoring';
/*** Completes feature bindings while preserving each component's explicit value contracts. */
export declare function finalizeFeatureMetadata(registry: ZoraComponentMetaRegistry): ZoraComponentMetaRegistry;
//# sourceMappingURL=finalizeFeatureMetadata.d.ts.map