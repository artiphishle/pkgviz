import { Button } from '../../../button/public';
const SELECTED_ICON = { name: 'checkmark' };
/*** Renders one Select option with shared web/native selection semantics. */
export function SelectOptionRow({ interactionPolicy, onSelect, option, selected, testID, }) {
    return (<Button color={selected ? 'primary' : 'neutral'} disabled={option.disabled} fullWidth interactionPolicy={interactionPolicy} onPress={() => onSelect(option.value)} testID={testID ? `${testID}-option-${option.value}` : undefined} trailingIcon={selected ? SELECTED_ICON : undefined} variant={selected ? 'soft' : 'ghost'}>
      {option.label}
    </Button>);
}
//# sourceMappingURL=SelectOptionRow.js.map