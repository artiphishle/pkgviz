import type React from 'react';
import type { SelectProps } from '../../../../types/select';
/*** Wraps Select presentation in the canonical Surface form-field semantics. */
export declare function SelectField<TValue extends string>({ children, props }: SelectFieldProps<TValue>): React.JSX.Element;
interface SelectFieldProps<TValue extends string> {
    children: React.ReactNode;
    props: SelectProps<TValue>;
}
export {};
//# sourceMappingURL=SelectField.d.ts.map