import type { ViewToken } from 'react-native';
import type { ManifestListProps } from '../../../../types/manifest-list';
/*** Keeps the native visibility callback stable and emits only serializable item identities. */
export declare function useListVisibility(onVisibleItemsChange: ManifestListProps['onVisibleItemsChange'], passive: boolean): ({ viewableItems }: {
    viewableItems: ViewToken[];
}) => void;
//# sourceMappingURL=useListVisibility.d.ts.map