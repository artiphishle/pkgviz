---
'pkgviz': patch
---

Use ZORA's readable graph fit with bounded spacing optimization, capped automatic label size and
dynamic manual zoom limits. Preserve exact optimized spacing in settings without a second layout.
Bound subpackage depth to the active package scope and enabled node categories after navigation.
