---
'pkgviz': patch
---

Consume the released Project Detector directory and file inventory so project parsing no longer performs a second filesystem traversal while preserving empty Tree directories.
