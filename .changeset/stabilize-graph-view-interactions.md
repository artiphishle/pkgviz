---
'pkgviz': patch
---

Fix GraphView interaction regressions by synchronizing project-tree navigation, preserving stable viewport behavior across controls, keeping compound dependency edges drawable and cycle edges visible, and letting the project tree fill the available sidebar height.
